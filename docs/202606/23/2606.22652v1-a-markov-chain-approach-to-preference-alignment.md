---
title: A Markov Chain Approach to Preference Alignment
title_zh: 偏好对齐的马尔可夫链方法
authors: "Takuya Koriyama, Tengyuan Liang"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22652v1"
tags: ["query:rl"]
score: 9.0
evidence: 使用马尔可夫链进行偏好对齐，替代RLHF方法
tldr: 针对现有对齐方法（RLHF将偏好简化为标量奖励、NLHF通过KL正则化极小极大优化保留成对效用）的不足，提出马尔可夫链MCHF，直接利用成对偏好定义转移核进行迭代对齐。理论证明MCHF几何收敛，速率由偏好的非传递性结构决定，且与NLHF在RLHF解附近一阶一致。该工作统一了基于奖励、博弈论和马尔可夫的偏好对齐视角，为生成模型对齐提供了简单有效的新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22652v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1235, \"height\": 468, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22652v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1181, \"height\": 555, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22652v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1526, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22652v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 597, \"height\": 423, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22652v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1687, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22652v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1654, \"height\": 499, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22652v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1717, \"height\": 1194, \"label\": \"Table\"}]"
motivation: RLHF将成对偏好简化为标量奖励损失信息，NLHF优化复杂，需更直接利用偏好结构的对齐方法。
method: "定义马尔可夫转移核P(x,dy)∝exp(U(x,y))μ_ref(dy)，从参考分布出发迭代采样直至平稳分布。"
result: MCHF几何收敛，速率由‖U‖_⊕度量；NLHF的镜像下降算法有类似保证；两者在RLHF解附近一阶一致。
conclusion: MCHF提供统一视角，首步即恢复RLHF列和奖励，后续迭代捕获非传递性结构，简化对齐过程。
---

## 摘要
我们提出基于人类反馈的马尔可夫链（MCHF），一种从成对人类偏好中对生成模型进行对齐的基本方法。与将比较简化为标量奖励的基于人类反馈的强化学习（RLHF）和通过KL正则化极小极大优化保留成对效用的基于人类反馈的纳什学习（NLHF）不同，MCHF直接使用成对偏好来定义模型输出上的转移机制。给定一个成对效用$U(x,y)$（量化人类对$y$相对于$x$的偏好）和一个参考概率分布$μ_{\mathsf{ref}}$，我们定义马尔可夫核$\mathsf{P}(x, dy)\propto \exp(U(x,y))μ_{\mathsf{ref}}(dy)$，并将从$μ_{\mathsf{ref}}$开始的马尔可夫链视为迭代对齐过程。我们证明MCHF几何级数快速收敛到平稳分布，收敛速度由半范数$\|U\|_\oplus=\inf_{g,f\in L^\infty(μ_{\mathsf{ref}})}\|U-g\oplus f\|_\infty$控制，该半范数量化了成对效用的非传递结构。我们进一步证明NLHF的镜像下降算法满足类似的结构自适应收敛保证。最后，通过扰动分析，我们证明当$\|U\|_\oplus$较小时，MCHF和NLHF在RLHF解附近一阶一致，这为基于奖励、博弈论和马尔可夫的对齐方法提供了统一视角。特别地，对于收敛到MCHF/NLHF均衡的两个自然算法，我们表明MCHF和NLHF的第一步恢复了基于列和奖励$\hat{f}(y)=\int μ_{\mathsf{ref}}(dx) U(x, y)$的RLHF解，而从第二次迭代开始，两个算法都包含了残差$U-(-\hat f)\oplus \hat f$的相同线性泛函，该残差捕捉了成对效用$U$的非传递结构。

## Abstract
We propose Markov Chain from Human Feedback (MCHF), an elementary approach for aligning generative models from pairwise human preferences. Unlike Reinforcement Learning from Human Feedback (RLHF), which reduces comparisons to a scalar reward, and Nash Learning from Human Feedback (NLHF), which preserves pairwise utilities through a KL-regularized minimax optimization, MCHF uses pairwise preferences directly to define a transition mechanism over model outputs. Given a pairwise utility $U(x,y)$, which quantifies human preference for $y$ over $x$, and a reference probability distribution $μ_{\mathsf{ref}}$, we define a Markov kernel $\mathsf{P}(x, dy)\propto \exp(U(x,y))μ_{\mathsf{ref}}(dy)$, and take the Markov chain starting from $μ_{\mathsf{ref}}$ as an iterative alignment procedure. We show that MCHF converges geometrically fast to the stationary distribution, with a convergence rate governed by the seminorm $\|U\|_\oplus=\inf_{g,f\in L^\infty(μ_{\mathsf{ref}})}\|U-g\oplus f\|_\infty$, which quantifies the non-transitive structure of the pairwise utility. We further show that a mirror-descent algorithm for NLHF satisfies an analogous structure-adaptive convergence guarantee. Finally, through a perturbation analysis, we prove that when $\|U\|_\oplus$ is small, MCHF and NLHF agree up to first order around an RLHF solution, which yields a unified view of reward-based, game-theoretic, and Markovian approaches to alignment. In particular, for two natural algorithms that converge to the MCHF/NLHF equilibria, we show that the first step of MCHF and NLHF recovers the RLHF solution based on the column-sum reward $\hat{f}(y)=\int μ_{\mathsf{ref}}(dx) U(x, y)$, and starting from the second iteration, both algorithms incorporate the same linear functional of the residual $U-(-\hat f)\oplus \hat f$, which captures the non-transitive structure of the pairwise utility $U$.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、深入、客观的中文总结。

### 论文总结：A Markov Chain Approach to Preference Alignment

#### 1. 论文的核心问题与整体含义

*   **研究动机与背景**：生成模型与人类偏好对齐是AI领域的核心问题。现有方法如**RLHF**（基于人类反馈的强化学习）将成对比较转化为单一标量奖励，但该过程通常依赖**Bradley-Terry-Luce (BTL) 模型**，假设偏好是“可传递的”，无法捕捉真实世界中可能存在的“非传递性”或“循环”偏好模式（如：A优于B，B优于C，C优于A）。另一种方法**NLHF**（基于人类反馈的纳什学习）保留了成对效用，但其求解过程是一个复杂的**KL正则化极小极大优化**，计算成本高，且其“悲观主义”的博弈论视角不一定是最自然的对齐范式。
*   **核心问题**：如何设计一种更直接、更简单、且理论完备的对齐方法，既能充分利用成对偏好信息（包括非传递性），又能避免RLHF的信息损失和NLHF的复杂优化。
*   **整体含义**：论文提出了**MCHF（基于人类反馈的马尔可夫链）**，作为一种全新的、基础的对齐框架。该方法直接利用成对偏好定义转移核，通过马尔可夫链的迭代达到对齐效果。论文统一了基于奖励 (RLHF)、博弈论 (NLHF) 和马尔可夫链 (MCHF) 三种对齐方法的视角，揭示了当偏好的非传递性较小时，三者之间存在紧密联系。

#### 2. 论文提出的方法论

*   **核心思想**：将对齐过程视为一个由偏好驱动的马尔可夫链。给定当前输出`x`，模型根据条件分布采样下一个输出`y`，该分布倾向于选择比`x`更受偏好的输出。

*   **关键技术细节**：
    *   **马尔可夫转移核定义**：给定成对效用函数`U(x,y)`（值越大表示`y`比`x`更受偏好）和参考分布$μ_{ref}$，定义转移核：
        `P(x, dy) = exp(U(x, y)) * μ_{ref}(dy) / Z(x)`，其中`Z(x)`是归一化常数。
    *   **迭代对齐过程**：从参考分布$μ_{ref}$开始，反复应用该转移核：
        $μ_{ref} → μ_{ref}P → μ_{ref}P^2 → … → μ_{ref}P^∞ = μ_{MC}$。最终的平稳分布$μ_{MC}$即为对齐后的模型分布。
    *   **关键概念：非传递性半范数**：论文引入半范数$\|U\|_⊕$衡量成对效用`U`的“非传递性”或“非加性”程度。当`U`可以写成`g(x) + f(y)`的加性形式时，$\|U\|_⊕ = 0$，此时所有三种方法的解都退化为RLHF解。
    *   **算法比较**：
        *   **RLHF**：学习标量奖励，然后将分布向其倾斜。
        *   **NLHF**：通过`μ_t^{NL} = argmax_μ [∫ μ_{t-1}^{NL}(dx)μ(dy)U(x,y) - KL(μ||μ_{ref})]`迭代求解纳什均衡。
        *   **MCHF**：通过`μ_t^{MC} = μ_{t-1}^{MC} P`迭代，直接利用偏好驱动的转移核。

*   **理论贡献**：
    1.  **结构自适应收敛**：证明MCHF的更新算子是一个压缩映射，收敛速率由半范数$\|U\|_⊕$决定，而非简单的无穷范数。这意味着算法能自动适应可加性结构，隐含地利用其进行快速收敛。
    2.  **NLHF的镜像下降算法**：提出一种新的镜像下降算法求解NLHF，当步长选择适当时，其收敛速率同样由$\|U\|_⊕$控制，优于依赖$\|U\|_∞$的先前工作。
    3.  **一阶等价性**：通过灵敏度和连续微分分析证明，当$\|U\|_⊕$很小时（即偏好接近可传递时），MCHF和NLHF的迭代动力学在RLHF解附近**一阶一致**。具体地：
        *   第一次迭代：两者都等价于用列和奖励$\hat{f}(y) = ∫μ_{ref}(dx)U(x,y)$进行RLHF。
        *   第二次及后续迭代：两者都吸收相同的线性泛函来修正非传递性部分，且第一阶对齐效应在**前两次迭代内**就已完成。

#### 3. 实验设计

*   **数据集/场景**：论文主要进行**理论分析和数值模拟验证**。没有使用任何真实世界的人类偏好数据集。
*   **实验设定**：设置了一个离散状态空间（|X| = 20）的合成场景。参考分布$μ_{ref}$为均匀分布。
*   **对比方法**：主要对比了**RLHF**、**NLHF**和**MCHF**三种方法在合成数据上的对齐结果。
*   **Benchmark**：以$\|U\|_⊕ \to 0$（即偏好逐渐变为可加性）时的理论预测（一阶近似）为标准，比较不同算法的实际密度。
*   **结果**：数值模拟验证了理论预测，即当$\|U\|_⊕$较小时，MCHF、NLHF与RLHF的一阶近似高度一致，且MCHF和NLHF本身也几乎重合。

#### 4. 资源与算力

*   **未明确说明**：这篇论文是**纯理论工作**，没有进行深度学习模型的大规模训练。实验部分是数值模拟，对计算资源需求很低。文中没有提及任何GPU型号、数量或训练时长等信息。

#### 5. 实验数量与充分性

*   **实验数量**：实验数量较少，仅包含一个用于验证理论结论的**合成数据模拟**。
*   **充分性评估**：从**理论验证**的角度看，实验设计是**充分且客观**的，因为它直接针对论文的核心理论发现（一阶等价性）进行验证。然而，从**工程实用性**的角度看，实验**非常不充分**。论文完全缺少：
    *   在真实人类偏好数据集（如Anthropic HH-RLHF, TL;DR等）上的实验。
    *   与现有方法（如DPO, PPO）在性能、效率上的对比。
    *   对MCHF方法实际实现中（如如何高效地从条件分布P(x, dy)采样）的量化评估。
    *   消融实验，以展示不同组件（如不同γ值的核）的影响。

#### 6. 论文的主要结论与发现

1.  **MCHF是一个新的、简单有效的对齐框架**：它通过马尔可夫链直接利用成对偏好进行迭代对齐，具有清晰的几何收敛保证。
2.  **收敛速率自适应**：MCHF的收敛由半范数$\|U\|_⊕$控制，表明它能自动适应偏好结构的非传递性程度，非传递性越小，收敛越快。
3.  **NLHF的收敛率改善**：提出了一种新的镜像下降算法，使得NLHF的收敛率也由$\|U\|_⊕$决定，优于先前工作。
4.  **统一了三种对齐方法**：在$\|U\|_⊕$很小的假设下（这在实践中常见，尤其是当偏好可近似为某种BTL模型时），RLHF、NLHF和MCHF在**一阶近似意义下是等价的**。这为理解不同类型的对齐方法提供了统一的理论框架。
5.  **对齐的早期动力学**：对齐的主要效果（包括非传递性部分的修正）可以在前两次迭代中捕获，为实际应用（尤其是计算成本高时）提供了重要的实用见解。

#### 7. 优点

*   **理论深度与严密性**：论文提供了极其深入和严格的理论分析，包括收敛率、灵敏度和统一视角。这为偏好对齐领域提供了坚实的基础。
*   **创新性**：将生成模型对齐与马尔可夫链理论联系起来，是一个新颖且优雅的视角。提出的MCHF方法简单、自然，直接来源于“偏好引导”的思想。
*   **统一视角**：不仅提出了新方法，更重要的是揭示了RLHF、NLHF和MCHF这三种不同思路的内在联系，为该领域提供了系统性理解。
*   **计算上的潜在优势**：论文指出，MCHF的条件采样器只需实现一次，后续迭代为“免训练”的推理过程，相比于NLHF每步都需要复杂优化的做法，在理论上具有潜在的计算优势。MCHF还能自然地支持推理时细化。

#### 8. 不足与局限

*   **缺乏实验**：最显著的限制是几乎**完全没有实验验证**。所有结论都基于理论推导和合成数据模拟。没有在真实规模、真实数据集上评估MCHF的性能，这严重削弱了其说服力和实用性。其声称的“简单”和“有效”缺乏实证支持。
*   **实际实现挑战**：论文承认从条件分布`P(x, dy)`采样是实际应用的核心挑战。虽然提及了rejection sampling、扩散模型等潜在解决方案，但未提供具体算法或实验结果。这看起来更像是一个“概念验证”工作，而非一个可以在工程中直接应用的方案。
*   **理论的局限性**：
    *   分析主要局限于$\|U\|_⊕$较小的**小扰动**情况。对于非传递性很强的偏好，其结论和一阶等价性可能不再成立。
    *   对于NLHF，达到理论上的最优收敛率需要预知或精确计算$\|U\|_⊕$，这在实践中可能非常困难，甚至不可行。
*   **风险与应用限制**：论文仅涉及理论建模，未讨论任何关于偏差、公平性或模型安全的议题。将这个方法应用于真实系统，其潜在的社会影响和伦理风险尚未被评估。

（完）
