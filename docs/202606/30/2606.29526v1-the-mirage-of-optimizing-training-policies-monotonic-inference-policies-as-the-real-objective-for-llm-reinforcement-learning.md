---
title: "The Mirage of Optimizing Training Policies: Monotonic Inference Policies as the Real Objective for LLM Reinforcement Learning"
title_zh: 优化训练策略的幻象：单调推理策略才是LLM强化学习的真正目标
authors: "Jing Liang, Hongyao Tang, Yi Ma, Yancheng He, Weixun Wang, Xiaoyang Li, Ju Huang, Wenbo Su, Jinyi Liu, Yan Zheng, Jianye Hao, Bo Zheng"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29526v1"
tags: ["query:rl"]
score: 9.0
evidence: 解决LLM强化学习中训练与推理不一致的问题
tldr: LLM强化学习中，训练和推理引擎的概率不一致导致离线策略问题和训练不稳定。现有研究忽略了训练策略优化未必改进推理策略的目标错位。本文提出单调推理策略改进（MIPI）目标，并设计MIPU框架，通过采样参考候选和推理侧差距代理选择性同步更新。实验表明，MIPU在高不匹配环境下显著提升推理性能与训练稳定性。该工作揭示了优化训练策略的幻象，为LLM后训练提供了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29526v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1431, \"height\": 204, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1460, \"height\": 237, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 744, \"height\": 388, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 905, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1492, \"height\": 314, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 496, \"height\": 343, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1491, \"height\": 326, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29526v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1491, \"height\": 409, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29526v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1492, \"height\": 459, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29526v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1359, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29526v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1059, \"height\": 1112, \"label\": \"Table\"}]"
motivation: 训练与推理引擎的概率不一致导致离线策略问题，且现有工作未解决策略优化目标错位，即训练策略更新不保证推理策略改进。
method: 提出MIPI目标确保推理策略单调改进，并设计MIPU两步框架：构造采样器参考候选，利用推理侧差距代理选择性地接受同步更新。
result: 在两种模型规模的高不匹配实验中，MIPU相比基线提升了平均推理性能，并维持了训练稳定性。
conclusion: MIPU有效解决了训练-推理目标错位，揭示了以推理策略为真正优化目标的必要性。
---

## 摘要
强化学习在大语言模型后训练中日益受到关注，但RL训练仍然脆弱，可能遭遇不稳定或崩溃。一个重要原因是训练-推理不匹配：LLM为了生成效率和训练精度，采用分离的推理和训练引擎，在实际中即使模型参数同步，同一轨迹在训练端和推理端的概率也不一致。这自然引入了一种特殊类型的离策略性，持续存在并毒害训练。先前的工作在解决离策略性以稳定不匹配下的训练策略方面做出了各种努力。本文指出，现有工作忽视了目标错位：对训练引擎中策略的有效更新并不保证推理策略（即部署中使用的策略）的改进。为此，我们为LLM RL提出了一种新的策略优化目标，称为单调推理策略改进。遵循这一原则，我们引入了单调推理策略更新，这是一个两步LLM RL框架，构建采样器参考的候选更新，并使用推理端间隙代理选择性接受同步候选。在两个模型规模下于高不匹配环境中进行的实验表明，MIPU提高了平均推理性能和训练稳定性。

## Abstract
Reinforcement learning (RL) has gained growing attention in large language model (LLM) post-training, yet RL training remains fragile and can suffer from instability or collapse. One vital cause is training-inference mismatch: LLM adopts separate inference and training engines for generation efficiency and training precision, which in practice exhibits inconsistent probabilities for the same trajectories on training and inference sides, even with synchronized model parameters. This naturally induces a special type of off-policyness ever existing and poisoning the training. Prior works have made various efforts in addressing the off-policyness to stabilize the training policies under the mismatch. In this paper, we point out the objective misalignment neglected by existing works that an effective update to the policy in the training engine not necessarily ensures the improvement of the inference policy, i.e., the one used in deployment. To this end, we propose a new policy optimization objective for LLM RL, named Monotonic Inference Policy Improvement (MIPI). Following this principle, we introduce Monotonic Inference Policy Update (MIPU), a two-step LLM RL framework that constructs sampler-referenced candidate updates and selectively accepts synchronized candidates using an inference-side gap proxy. Experiments conducted on two model scales under high mismatch show that MIPU improves average reasoning performance and training stability.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：强化学习（RL）在大语言模型（LLM）后训练中应用广泛（如 DeepSeek-R1、GRPO、DAPO等），用于提升推理、指令遵循等能力。现代 LLM RL 管线出于效率考虑，通常将生成（rollout）与梯度计算分离：推理引擎（如 vLLM、SGLang）用于采样，训练引擎（如 FSDP、Megatron）用于计算 log‑probability 和更新。即使参数同步，由于精度、解码实现、服务后端等差异，同一轨迹在训练引擎中的策略 π 与推理引擎中的策略 μ 会分配不同概率，导致**训练‑推理不匹配**。
- **核心问题**：现有工作（如 TIS、MIS、LR‑decay、FP16 量化等）主要从系统层面或优化层面降低不匹配，但忽视了**目标层级错位**：训练策略的有效更新并不保证推理策略（即实际部署用的策略）也改进。即 J(π_{k+1}) - J(π_k) ≥ 0  ⇏  J(μ_{k+1}) - J(μ_k) ≥ 0。这种“优化训练策略的幻象”会导致训练不稳定甚至崩溃。
- **研究意义**：本文首次从目标层面正视该问题，提出应将推理策略的单调改进作为真正优化目标，而非仅纠正训练侧的各个项。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
提出**单调推理策略改进（MIPI）** 原则，即策略优化应以推理策略 μ 的性能单调提升为目标。基于该原则设计两步框架 **单调推理策略更新（MIPU）**。

### 关键技术细节
1. **MIPI 分解**：将推理策略改进 ΔJ(μ) = J(μ_{k+1}) - J(μ_k) 分解为三项：
   - ① 更新后推理间隙：J(μ_{k+1}) - J(π_{k+1})
   - ② 训练侧更新：J(π_{k+1}) - J(π_k)
   - ③ 更新前推理间隙：J(π_k) - J(μ_k)
   
2. **Step 1：采样器参考策略更新（处理 ②+③）**  
   - 目标优化 J(π_{k+1}) - J(μ_k)，等价于 Δ(π_{k+1}, μ_k)。使用**采样器参考的截断权重**（TIS 风格）：  
     - 将训练‑推理比例分解：π_θ/μ_k = (π_k/μ_k) · (π_θ/π_k)
     - 对预更新不匹配权重 w_k = π_k/μ_k 进行截断（设置上限 w_max），仅对当前更新比例 r(θ)=π_θ/π_k 应用 PPO 裁剪。
     - 损失函数：J_{S1}(θ) = E[ (1/G) Σ_i min( ¯w_k · r_i(θ)ˆA_i , clip(r_i(θ))·ˆA_i ) ]
   - 优化后得到候选训练策略 π_{k+1}，随后同步到推理引擎得到 μ_{k+1}。

3. **Step 2：推理间隙感知的接受规则（处理 ①）**  
   - 估计后更新推理间隙代理 ˆT_post = - E_{μ_{k+1}}[ ρ_i · ˆA_{μ_{k+1}, i} ]，其中 ρ_i 为长度归一化的重要性采样比。
   - 接受条件：ˆT_post ≥ -c （c 为容忍参数）。若满足则接受候选，否则回滚到上一个检查点。
   - 使用动态容忍度：训练前期宽松（有利于早期探索），后期收紧（保护稳定）。

### 算法流程（文字说明）
- 输入：基础模型、组大小 G、裁剪范围 ϵ、容忍度 c。
- 初始化训练策略 π_θ0 = π_base，推理策略 μ_0 = Sync(π_θ0)。
- 对于每一步 k：
  1. 保存检查点。
  2. **Step 1**：从 μ_k 采样训练批次，计算奖励、组相对优势 ˆA_{μ_k} 和不匹配权重；最大化 Step 1 损失；更新参数得 π_{k+1}，同步得 μ_{k+1}。
  3. **Step 2**：从 μ_{k+1} 采样验证批次，计算 ˆT_post；若 ˆT_post < -c，则回滚模型和优化器状态到上一步检查点。
- 继续下一轮迭代。

## 3. 实验设计

- **数据集**：
  - 训练数据：DAPO-Math-17k（过滤后 Qwen3-1.7B 用 5759 例）和 DeepMath-103K（过滤后 Qwen3-4B 用 1491 例）。过滤条件：基础模型能解但非饱和成功率，确保有意义的奖励方差。
  - 评估数据集：5 个数学推理基准：MATH-500、AIME24、AMC23、Minerva、OlympiadBench。对 AIME24 和 AMC23 使用 avg@16 降低方差。
- **Benchmark**：pass@1 准确率；同时报告训练稳定性（是否在后训练中崩溃或急剧退化）。
- **对比方法**：
  - 基线（Baseline）：标准 GRPO（带双裁剪）。
  - MIS：基于 token 级不匹配过滤（掩码超出阈值 2 的 token）。
  - LR‑decay：间隔减半学习率（Qwen3-4B 每 234 步，Qwen3-1.7B 每 249 步）。
  - Ours（MIPU）。
  - 消融：+ Step 1 仅，+ Step 2 仅，完整 MIPU。
- **高不匹配设置**：采用 FP8 量化推理进行 rollout，因量化放大训练‑推理不匹配。

## 4. 资源与算力

- 文中明确提及：使用 **8 块 H100 GPU**，RL 框架为 ROLL，训练引擎为 Megatron，推理引擎为 vLLM。
- 训练具体时长未明确说明，但实验包含两个模型规模（Qwen3-4B 和 Qwen3-1.7B）各多步，并进行了消融、敏感性分析等。算力资源属于中等规模。

## 5. 实验数量与充分性

- **主要实验**：两个模型（1.7B 和 4B）× 4 种方法（基线、MIS、LR‑decay、Ours）× 5 个基准 → 每模型 20 组结果（表 1）。同时包含稳定性标注（✓/✗）。
- **消融实验**（Qwen3-4B 下）：4 种变体（基线、+Step1、+Step2、完整）× 5 基准 → 表 2，并附训练曲线（奖励、K3-KL、推理间隙、回滚率）— 图 3。
- **附加分析**：推理间隙信号分析（图 4a）、与随机回滚对比（图 4b）、Step 1 实现变体对比（PPO-IS、Vanilla-IS、TIS，图 5）、容忍度敏感性（图 6）。
- **充分性评价**：实验覆盖主结果、消融、机制分析、敏感性，多角度验证。对比基线合理，包括了常见不匹配处理方法。但仅限两个中等规模模型（≤4B），未见更大模型验证。实验设计较为客观公平：使用相同评估协议、控制训练数据与超参数。

## 6. 主要结论与发现

1. **核心结论**：训练‑推理不匹配不仅是一个系统偏差，更会改变策略优化的目标对象。以训练策略改进为目标可能导致推理策略退化，因此应将推理策略的单调改进作为真正目标。
2. **方法有效性**：MIPU 在高不匹配的 FP8 量化 rollout 设置下，在两个模型上均取得最高平均 pass@1 准确率（Qwen3-4B: 66.71%，Qwen3-1.7B: 53.97%），并保持训练稳定（无后期崩溃），而对比方法（基线、MIS、LR‑decay）均出现不稳定或退化。
3. **组件互补性**：Step 1（采样器参考更新）提升候选质量；Step 2（推理间隙感知接受）过滤不可靠同步候选。两者缺一不可。
4. **推理间隙信号有效性**：比随机回滚更优，因为其基于结构化的不匹配信号而非盲目减少更新数。

## 7. 优点

- **视角创新**：首次从目标层级审视训练‑推理不匹配，指出“优化训练策略的幻象”，提出应以推理策略为优化主体。
- **理论分解清晰**：将推理策略改进分解为三项，并对应设计两步操作，逻辑严谨。
- **方法灵活**：Step 1 和 Step 2 均可灵活替换（如不同采样器校正方法、不同后更新间隙估计器）。
- **实验设计全面**：包括主结果、消融、机制验证、敏感性分析；在高不匹配量化场景下检验；对比基线较新（TIS、MIS、LR‑decay）。
- **稳定性验证充分**：不仅看峰值，还关注训练后期是否崩溃，并给出稳定性标记。

## 8. 不足与局限

- **模型规模有限**：实验仅限 Qwen3-1.7B 和 Qwen3-4B，未在更大模型（如 7B、14B、72B）或更广泛模型族上验证，泛化性存疑。
- **算力约束**：作者明确指出由于计算资源限制，实验仅到中等规模。未来需在更大模型和更多 RL 训练系统上验证。
- **Step 2 实现为启发式**：后更新间隙代理是近似估计（基于反向身份 + 长度归一化重要性采样），理论上无严格单调改进保证，依赖容忍度参数调优。
- **适用场景**：主要针对数学推理等具有确认奖励的任务；其他领域（如对话、代码生成）中的不匹配问题未涉及。
- **未与所有基础设施方案比较**：如不直接对比 FP16 或系统级优化方法（如 QuRL），但作者指出目标正交，这是合理的。
- **数据集过滤细节**：训练数据过滤规则（非平凡但非饱和成功率）可能影响泛化，未分析过滤敏感性。

（完）
