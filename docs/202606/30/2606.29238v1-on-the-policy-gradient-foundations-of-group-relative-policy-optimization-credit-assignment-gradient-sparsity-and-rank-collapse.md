---
title: "On the Policy Gradient Foundations of Group Relative Policy Optimization: Credit Assignment, Gradient Sparsity, and Rank Collapse"
title_zh: 论群体相对策略优化的策略梯度基础：信用分配、梯度稀疏性与秩坍塌
authors: "Amritansh Mishra, Supriyo Chakraborty, Berkcan Kapusuzoglu"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29238v1"
tags: ["query:rl"]
score: 9.0
evidence: 针对LLM语言生成的GRPO策略梯度分析
tldr: GRPO通过组内平均奖励消除了critic，但本文证明其存在根本性信用分配缺陷：在输出级奖励下，每个token获得相同优势，导致token级信用坍缩为标量。理论推导和Nemotron-4B/GSM8K实验显示梯度矩阵有效秩恒为2，归因于优势零和约束。该工作揭示了GRPO在多步推理中的关键限制，并给出了基线最优的条件。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29238v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 824, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29238v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 829, \"height\": 544, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29238v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 828, \"height\": 543, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29238v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 728, \"height\": 212, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29238v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1094, \"height\": 279, \"label\": \"Table\"}]"
motivation: 分析GRPO信用分配失效的理论根源及其对梯度结构的影响。
method: 从策略梯度定理严格推导GRPO，并结合SVD分解实验验证梯度矩阵的秩峰化。
result: 梯度矩阵有效秩约为2，不随组大小变化，源于优势的零和约束。
conclusion: GRPO的信用分配瓶颈是其多步推理的主要限制，基线合理性有条件约束。
---

## 摘要
群体相对策略优化通过使用分组轨迹的平均奖励作为基线，消除了PPO中学习的评论家。我们从策略梯度定理的第一性原理出发，对GRPO进行了严格的推导，揭示了一个基本的信用分配失败：在仅输出奖励的情况下，轨迹中的每个令牌获得相同的优势，将令牌级别的信用坍缩为一个标量。我们证明这会导致训练过程中梯度稀疏性加剧，并通过Nemotron-4B/GSM8K上GRPO梯度的SVD分析经验性地表明，无论组大小R∈{2,4,8}如何，梯度矩阵的有效秩≈2。我们将此形式化为由优势的零和约束引起的内在秩2结构，并推导了GRPO基线最优的条件。我们的结果刻画了GRPO的简单性在理论上何时合理，并确定信用分配瓶颈是多步推理的关键限制。

## Abstract
Group Relative Policy Optimization (GRPO) eliminates the learned critic in PPO by using the mean reward of grouped rollouts as a baseline. We provide a rigorous derivation of GRPO from first principles of the policy gradient theorem, revealing a fundamental credit assignment failure: under output-only reward, every token in a rollout receives identical advantage, collapsing token-level credit to a single scalar. We prove this induces gradient sparsity that intensifies over training, and demonstrate empirically via SVD analysis of GRPO gradients on Nemotron-4B/GSM8K that the gradient matrix has effective rank $\approx$ 2 regardless of group size $R \in \{2, 4, 8\}$. We formalize this as an intrinsic rank-2 structure arising from the zero-sum constraint on advantages and derive conditions under which GRPO's baseline is optimal. Our results characterize when GRPO's simplicity is theoretically justified and identify the credit assignment bottleneck as the key limitation for multi-step reasoning.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：GRPO 作为一种无需学习评论家的策略梯度变体，在 DeepSeek‑R1 等大模型后训练中取得显著成功，但其理论基础——特别是信用分配机制和梯度结构——尚未被系统理解。
- **核心问题**：GRPO 是否真的能实现合理的令牌级信用分配？其梯度信号是否存在内在的瓶颈？这些瓶颈对多步推理任务有何影响？
- **整体含义**：论文通过严格推导证明了 GRPO 在输出级奖励下存在根本性的信用分配失败——每个令牌获得相同的优势，导致令牌级信用坍缩为单个标量。这进而引发了梯度稀疏性和秩坍塌（有效秩≈2），从根本上限制了 GRPO 对多步推理的细粒度学习能力。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：从策略梯度定理出发，逐步推导出 GRPO，识别其两个关键特化：
  1. **输出级奖励（ORM）**：奖励仅在终端状态给出，且 γ=1，导致任意时间步 t 的回报均为终端奖励 R_terminal，因此所有 token 的回报相同。
  2. **组平均基线**：用同一提示下 R 条轨迹的奖励均值作为基线，替代需要学习的价值函数。
- **关键技术细节**：
  - 推导出 GRPO 梯度为：
    \[
    \hat{g}_{\text{GRPO}} = \frac{1}{R}\sum_{i=1}^{R} \underbrace{(r_i - \hat{v}_R)}_{\text{标量优势}} \cdot \underbrace{\sum_{t=0}^{T_i-1} \nabla_\theta \log \pi_\theta(a_t^{(i)} | s_t^{(i)})}_{\text{整条轨迹的梯度方向}}
    \]
    其中优势 \(\hat{A}_i\) 是一个与 token 位置无关的标量。
  - **理论证明**：定理 3.1 证明每条轨迹内所有 token 获得相同的优势，即 \( \hat{A}_i(t) = \hat{A}_i \)。
  - **梯度因子化**：优势标量提取后，梯度可写为 \(\frac{1}{R}\sum_i \hat{A}_i \psi_i\)，其中 \(\psi_i\) 是轨迹 i 的所有对数梯度之和。这解释了低秩结构：每条轨迹只贡献一个方向。
- **优势的结构约束**：定理 4.1 证明组内优势满足零和、负相关、方差受限于奖励方差等性质，并且只有“有效”轨迹（\(|\hat{A}_i|>\epsilon\)）才贡献显著梯度。
- **多轮偏差分析**：多轮设置下，GRPO 必须使用 v(s0) 而非 v(sh)，导致偏差随阶段线性增长。

## 3. 实验设计：数据集、场景、基准方法与对比

- **数据集与场景**：GSM8K（数学推理任务），使用二元正确性奖励（r∈{0,1}）。
- **模型**：Nemotron-4B（NVIDIA 出品的 4B 参数语言模型）。
- **基准方法**：仅 GRPO 本身，但对比不同组大小 R ∈ {2,4,8}，并分析梯度谱特性（无外部对比方法，重点在自身体系分析）。
- **对比方式**：主要对比不同 R 下梯度矩阵的有效秩、top‑1 奇异值占比、训练准确率。未与 PPO、REINFORCE 或其他基线进行端到端性能比较。

## 4. 资源与算力

- **文中明确说明**：使用 FSDP 训练流水线，对跟踪的线性层（如 q_proj, v_proj）每 50 步进行一次全梯度 SVD 跟踪。但**未给出具体 GPU 型号、数量、训练时长或总浮点运算量**。实验规模较小（仅 4B 参数模型、一个数据集），属于理论验证性质。

## 5. 实验数量与充分性

- **实验组数**：
  - 三个组大小 R=2,4,8，每个下重复 3 个随机种子。
  - 报告了有效秩、top‑1 分数、训练准确率的曲线（图 1–3）和一张汇总表（表 1）。
- **充分性评价**：
  - **正面**：针对核心理论预测（有效秩≈2）提供了清晰的验证，结果与零和优势理论一致。
  - **局限**：仅在一个模型（Nemotron-4B）和一个任务（GSM8K）上测试，缺乏不同规模模型、不同奖励函数（如连续奖励、多步过程奖励）的消融。也未与 PPO、REINFORCE 等基线进行性能对比。实验主要关注机制性验证，而非全面 benchmark。

## 6. 论文的主要结论与发现

- **GRPO 是 REINFORCE 的一个特殊变体**，其梯度结构源于两个特化：输出级奖励和组平均基线。
- **令牌级信用完全坍缩**：每个轨迹内所有 token 获得相同优势，无法区分哪些 token 对结果至关重要。
- **梯度有效秩≈2 且不随 R 增加**：无论使用 2 条还是 8 条轨迹，梯度矩阵始终只提供约两个独立更新方向。R 增大可改善优势估计方差，但不会增加梯度信息的维度。
- **梯度信号随训练衰减**：当策略收敛（对某个提示全部正确或全部错误）时，奖励方差 σr → 0，所有优势 → 0，该提示贡献零梯度。因此 GRPO 自然聚焦于策略边界处的样本。
- **多轮设置存在根本性偏差**：因为无法计算中间状态值，GRPO 的偏差随阶段线性增长，不适合多步推理。

## 7. 优点

- **理论推导严谨且透明**：从策略梯度定理出发，逐步推导出 GRPO，清晰指出每一步的近似和假设（ORM、组平均基线），没有隐藏的隐式假设。
- **发现并提供理论解释**：发现“有效秩≈2”的惊人现象，并用零和优势因子化结构提供了简明解释。
- **实验验证直指核心**：SVD 分析直接测量梯度矩阵的谱，有力证实了理论预测。
- **对使用者的实用指导**：明确指出 GRPO 的适用条件（单步、高初始奖励方差）与失效场景（多步推理）。帮助从业者决定何时选用 GRPO。
- **开源友好**：附录包含零和、方差、协方差、Hoeffding 界等详细证明，便于复现和扩展。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅一个模型（Nemotron-4B）和一个任务（GSM8K）。不同模型规模、不同领域、不同奖励函数（如连续值、PRM）下的行为未知。
  - 未与 PPO、RLOO、DPO 等基线比较最终性能，缺乏量化信用分配改进带来的收益或损失。
- **忽略裁剪和 KL 正则化**：论文明确声称只关注梯度核心，但这些组件在实践中会显著影响优化方向和收敛行为，可能导致理论预测与实际梯度的偏差。
- **多轮分析缺乏实证**：多轮偏差的推导仅停留在理论层面，未在类似 Multi‑Turn GSM8K 或交互式任务中实际验证。
- **秩坍塌的长期动态未测**：论文仅观察训练过程中的快照，未展示秩随策略收敛而进一步衰减的演变（尽管理论预测如此）。
- **资源细节缺失**：未披露 GPU 型号、数量、训练时间，不利于可重复性。
- **风险提示**：论文结论可能被过度泛化。例如，对于使用过程奖励或非零 γ 的变体，有效秩可能不恒为 2。作者也承认未来可通过自适应基线或密集奖励打破瓶颈，但并未验证。

（完）
