---
title: Reinforcement Learning without Ground-Truth Solutions can Improve LLMs
title_zh: 无真实解强化学习可提升大语言模型
authors: "Yingyu Lin, Qiyue Gao, Nikki Lijing Kuang, Xunpeng Huang, Kun Zhou, Tongtong Liang, Zhewei Yao, Yi-An Ma, Yuxiong He"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27369v1"
tags: ["query:rl"]
score: 9.0
evidence: 无需真值解的LLM强化学习奖励框架
tldr: "现有强化学习训练LLM需要真实解，限制了应用场景。本文提出RiVER框架，利用执行反馈作为连续奖励，并针对奖励未校准问题设计实例级比较和排名校准。在AtCoder等评分基准上，RiVER提升8.9%-9.4%；在LiveCodeBench和USACO等精确解基准上，分别提升2.4%和3.5%。结果表明，无需真实解，仅基于分数优化即可有效提升LLM通用编码能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27369v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1694, \"height\": 841, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27369v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 608, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27369v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1687, \"height\": 957, \"label\": \"Table\"}]"
motivation: 现有强化学习训练LLM依赖真实解，限制了其在无真实解任务中的应用。
method: RiVER使用执行反馈作为连续奖励，通过实例级比较和排名校准解决规模主导和频率主导问题。
result: "RiVER在ALE评分排名提升8.9%和9.4%，在LiveCodeBench和USACO提升2.4%和3.5%。"
conclusion: 基于分数优化的训练无需真实解，可有效增强LLM通用编码能力。
---

## 摘要
使用可验证奖励的强化学习（RLVR）训练大语言模型通常依赖真实答案来分配奖励，限制了其在未知真实解任务中的适用性。我们提出一个名为RiVER（Ranking-induced VERifiable framework）的框架，该框架在没有真实解的情况下，利用确定性执行反馈作为连续值监督信号，在基于分数的优化任务上训练LLM。将群体相对强化学习应用于此类连续奖励时，我们发现了两个关键挑战："尺度主导"，即跨测试实例未校准的分数幅度扭曲了策略更新；以及"频率主导"，即反复采样的次优解可能压过罕见但更强的候选解。RiVER通过校准的奖励塑形应对这些挑战，该方法使用实例级比较并强调排名靠前的求解器，同时为其他有效解保留有界反馈。我们在12个AtCoder Heuristic Contest任务上训练，并在Algorithm Engineering Benchmark（ALE-Bench）、LiveCodeBench和USACO上评估。RiVER使Qwen3-8B和GLM-Z1-9B-0414在ALE评分排名中分别提升了8.9%和9.4%。更重要的是，尽管仅在基于分数的任务上训练且没有任何真实解，RiVER在LiveCodeBench和USACO等精确解基准上也将骨干模型的性能平均绝对提升了2.4%和3.5%。相比之下，使用原始执行分数训练的基线模型虽然提升了ALE评分，却无法迁移到精确解基准。这些结果表明，结合适当的奖励校准，基于分数的优化任务可以在没有真实解的情况下作为通用编程能力的有效训练环境。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) for training LLMs typically rely on ground-truth answers to assign rewards, limiting their applicability to tasks where the ground-truth solution is unknown. We introduce a \textbf{R}anking-\textbf{i}nduced \textbf{VER}ifiable framework (RiVER) that trains LLMs on score-based optimization tasks without ground-truth solutions, using deterministic execution feedback as continuous-valued supervision. When applying group-relative RL to such continuous rewards, we identify two key challenges: \emph{scale dominance}, where uncalibrated score magnitudes across test instances distort policy updates, and \emph{frequency dominance}, where repeatedly sampled suboptimal solutions can outweigh rare but stronger candidates. RiVER addresses these challenges with calibrated reward shaping that uses instance-wise comparisons and emphasizes top-ranked solvers while retaining bounded feedback for other valid solutions. We train on 12 AtCoder Heuristic Contest tasks and evaluate on Algorithm Engineering Benchmark (ALE-Bench), LiveCodeBench, and USACO. RiVER advances Qwen3-8B and GLM-Z1-9B-0414 by 8.9\% and 9.4\% in ALE rating rank. More importantly, despite training exclusively on score-based tasks without any ground-truth solutions, RiVER also improves the backbones across exact-solution benchmarks such as LiveCodeBench and USACO by an absolute average improvement of 2.4\% and 3.5\%. By contrast, baselines trained with raw execution scores improve ALE rating but fail to transfer to exact-solution benchmarks. These results suggest that score-based optimization tasks, combined with proper reward calibration, can serve as effective training environments for general coding ability without ground-truth solutions.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有主流的大语言模型（LLM）强化学习训练（RLVR）通常依赖“真实解”（ground-truth answers）来分配奖励（如数学答案匹配、代码单元测试通过/不通过）。这限制了RLVR在那些没有唯一真实解的开放性问题中的适用性，例如算法设计、优化、规划等任务，这些任务可能有多个可行解且质量不一，最优解未知或不可计算。
- **核心问题**：能否利用“无真实解但可验证”的环境——即候选解可通过执行检查可行性并通过目标函数比较质量——作为有效的训练信号，从而提升LLM的通用推理和编码能力？
- **整体含义**：论文提出，基于分数的优化任务（如AtCoder Heuristic Contest）可以作为可扩展的无真值监督源，关键在于如何将原始的、未校准的分数转化为稳定的强化学习信号。这代表着从“答案匹配”到“相对比较”的范式转变。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 提出 **RiVER（Ranking-induced VERifiable Reinforcement Learning）** 框架，将无真实解的可执行优化任务中的确定性执行反馈转化为稳定的策略梯度信号。
- 具体方法分为三步：**实例级排序** → **获胜者加权奖励塑形** → **跨实例聚合**，然后使用GRPO算法进行优化。

### 关键技术细节
1. **实例级排序（Instance-wise Ranking）**：
   - 对于每个隐藏测试实例 \( T_m \)，将G个候选求解器按该实例上的目标分数排名（有效候选间）。无效候选单独处理。
   - 采用平均并列排名（average-tie ranking），避免重复高分被过度奖励。
   - 排名消除了原生分数因实例规模、图结构等造成的幅度差异（解决“尺度主导”问题）。

2. **获胜者加权奖励塑形（Winner-heavy Reward Shaping）**：
   - 对每个求解器-实例对赋予奖励 \( s_{i,m} \)：
     - 无效解：-1
     - 第一名（唯一或并列按平均排名处理）：1
     - 其他有效解：在 \([-0.5, 0.5]\) 区间内线性映射
   - 这样拉开获胜解与其余解的距离，同时保留非获胜有效解的差异化反馈，避免重复的次优模式在梯度中占据主导（解决“频率主导”问题）。

3. **聚合与GRPO更新**：
   - 计算每个候选求解器在所有实例上奖励的平均值 \( \bar{s}_i \)，作为其组内优势 \( \hat{A}_i \)。
   - 直接用于GRPO的裁剪目标和KL正则化，不再额外标准化。

### 算法流程简述
- 对每个问题：采样G个响应 → 提取程序 → 在相同隐藏实例上执行 → 获取可行性/分数 → 按实例排序 → 塑形奖励 → 聚合 → 用GRPO更新策略。

## 3. 实验设计：数据集、基准、对比方法

### 训练环境
- 从 **AtCoder Heuristic Contest (AHC)** 选出12个任务（AHC047–AHC062中排除4个不兼容任务），作为无真值优化训练集。
- 每个任务包含问题描述、官方评估器、测试实例生成器。
- 每个候选程序在10个隐藏测试实例上执行，输出分数作为执行适应度。

### 评估基准
- **分数型基准**：ALE-Bench（基于AHC问题构建的算法工程基准），报告 Rating（越高越好）和 Rank%（越低越好）。
- **精确解基准**：
  - LiveCodeBench v5 和 v6（按难度分Easy/Med/Hard）
  - USACO（奥林匹克编程）
  报告平均Pass@1准确率（三次独立运行）。

### 对比方法（基线）
基于相同的骨干模型（Qwen3-8B 和 GLM-Z1-9B-0414），比较以下奖励设计：
- **Raw-GRPO**：直接使用原始执行分数聚合，组内标准化后GRPO。
- **RS-GRPO**：风险敏感变换代替组标准化。
- **Raw-Binary**：最高分得1，其余0（胜者全得）。
- **Instance-Norm**：实例内单独标准化分数再平均。
- **Rank-uniform**：同实例排序后均匀分配奖励（不偏袒获胜者）。
- **RiVER**：本文方法（获胜者加权）。

## 4. 资源与算力

- 论文中**未明确说明**使用的GPU型号、数量或训练时长。
- 只提到优化器为AdamW，学习率1e-6，KL系数0.001，组大小G=16，全局批次大小64。
- 未提供训练步数或总时间等信息。

## 5. 实验数量与充分性

- **主要实验结果**（表1）：基于两个骨干模型，在5个评估指标（ALE Rating、LCB v5、LCB v6、USACO）上报告了完整的对比。
- **消融实验**：通过基线对比，系统地剥离了原始分数、实例级排序、获胜者加权等组件的影响，验证了各组件必要性。
- **动态分析**（图2）：展示了12个AHC训练任务上“截至目前最佳”分数的变化曲线，比较RiVER与Raw-GRPO。
- **案例分析**：深入分析AHC057任务中模型生成的求解器质量，定性说明了策略改进。
- **信息论分析**：提供了组相对反馈分辨率的理论界定。

**充分性评价**：实验设计较为全面，覆盖了多个基准和多阶段消融。但缺少在不同规模骨干模型（如7B、14B、72B）上的验证，也未进行超参数敏感性分析或不同训练数据量的实验。训练环境局限于AHC任务，虽然结论声称迁移到其他基准，但可能受任务特定性影响。

## 6. 论文的主要结论与发现

- **RiVER在无真实解训练条件下，同时提升了分数型基准（ALE-Bench）和精确解基准（LiveCodeBench、USACO）的性能**。Qwen3-8B的ALE Rating提升142分（+8.9%），GLM-Z1-9B提升157分（+9.4%）；精确解基准平均提升2.4%和3.5%。
- **原始执行分数直接优化（Raw-GRPO等）虽能提升ALE评分，但无法迁移到精确解基准**，表明需要恰当的奖励校准。
- **仅实例级排序（Instance-Norm、Rank-uniform）不足以带来可迁移收益**，必须结合获胜者加权塑形才能实现跨领域提升。
- **提升主要集中在中等和困难问题上**（LiveCodeBench分析），表明训练帮助模型应对更复杂的推理。
- **信息论分析**表明，分数型环境提供更高的反馈分辨率，降低组内所有样本反馈相同的概率，从而提供更丰富的学习信号。

## 7. 优点：方法或实验设计上的亮点

- **问题新颖**：首次系统性地将没有真实解的可执行优化任务用于RLVR训练，并展示了向精确解基准的迁移能力，具有实际和理论价值。
- **方法简洁有效**：RiVER仅通过两种设计（实例级秩变换+获胜者加权塑形）解决了两个关键陷阱，无需复杂的状态价值网络或过程奖励模型。
- **实验设计严谨**：对比了5种不同奖励设计的基线，从原始分数到逐步加入排名和加权，清晰地隔离了每个因素的影响。
- **迁移验证**：不仅在同类任务（ALE-Bench）上评估，还跨域测试了标准编程竞赛（LiveCodeBench、USACO），增强了结论的泛化性。
- **理论与实践结合**：提供了组反馈碰撞的命题，从信息论角度解释了为何分数型环境有利于群体相对学习。

## 8. 不足与局限

- **算力资源未公开**：无法判断训练成本的可复制性和可行性。
- **骨干模型规模有限**：仅测试了8B和9B级别模型，未在更大模型（如70B）或更小模型（如1.5B）上验证，缩放规律未知。
- **训练数据单一**：仅使用12个AHC任务，尽管任务多样，但仍源自同一比赛平台，可能引入领域偏差。其通用性需更多不同来源的优化任务验证。
- **评估基准的时间相关性**：训练使用的AHC任务日期晚于ALE-Bench cutoff，但LiveCodeBench和USACO可能存在时间重叠或间接知识泄露问题（论文声明严格分离，但无法完全排除）。
- **缺乏对超参数敏感性的分析**：如组大小G、KL-beta系数等的影响未探讨。
- **仅报告Pass@1指标**：对于编程任务，Pass@k或聚合多样本表现可能更鲁棒，但未提供。
- **定性分析深度有限**：案例分析仅聚焦一个任务（AHC057），代表性不足。
- **失败模式讨论不足**：没有分析哪些类型的优化任务或模型规模下RiVER效果较差，局限性分析不够。

（完）
