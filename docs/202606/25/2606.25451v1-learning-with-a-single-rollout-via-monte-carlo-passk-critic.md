---
title: "Learning with a Single Rollout via Monte Carlo Pass@k Critic"
title_zh: "通过蒙特卡洛Pass@k评判器的单次 rollout 学习"
authors: "Fengdi Che, Yang Liu, Lei Yu, Meng Cao, Tong Che, Rupam Mahmood, Dale Schuurmans"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25451v1"
tags: ["query:rl"]
score: 9.0
evidence: 单次轨迹PPO结合蒙特卡洛评论家用于语言模型强化学习中的令牌级信用分配
tldr: "语言模型强化学习中，基于组内归一化的优势估计因重复采样成本高且轨迹异构而效果有限。本文提出SR-PPO，通过单次rollout训练一个校准的token级信用评论家，预测Pass@k成功概率，实现细粒度信用分配。当k增大时，Pass@k趋近于可达性指标，可在O(|V|+|E|)时间内计算。在数学推理基准HMMT26和AIME24上，SR-PPO在Pass@128成功率上取得一致提升，且学习动态稳定。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25451v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1439, \"height\": 873, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1455, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1428, \"height\": 350, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1366, \"height\": 757, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25451v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1376, \"height\": 901, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25451v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1467, \"height\": 222, \"label\": \"Table\"}]"
motivation: 现有RL方法因重复采样成本高和轨迹异构难以实现有效的token级信用分配，亟需一种低成本的单rollout优势估计方法。
method: "提出SR-PPO，利用单次rollout的Monte Carlo结果训练评论家预测Pass@k成功概率，作为token级信用信号，折扣易解前缀而聚焦硬前缀。"
result: "随k增大，Pass@k收敛为可达性指标，可在图上线性时间计算；在HMMT26和AIME24上，SR-PPO的Pass@128成功率显著提升。"
conclusion: SR-PPO以单rollout实现了稳定的token级信用分配，降低了计算成本，为语言模型RL提供了一种高效替代方案。
---

## 摘要
在语言模型的强化学习（RL）中，估计 token 级别的优势仍然具有挑战性，因为扩展 episodic 经验收集的成本很高。对于基线优势估计方法，这种困难更加严重，因为重复采样会导致轨迹发散为实质上不同的推理前缀。在这种情况下，像GRPO这样的RL算法证明是有限的：结果奖励过于稀疏，无法归因于像中间步骤这样的特定动作，并且跨采样轨迹的比较并不简单，因为它们是异质的。为了减轻重复采样的计算成本和信用分配的困难，我们研究了单 rollout 近端策略优化（SR-PPO），它在语言模型的RL中实现了 token 级别的信用分配。我们不是通过归一化候选组内的episodic回报来估计优势，而是使用每个提示的一次 rollout 的蒙特卡洛结果来训练一个校准的 token 级别信用评判器。具体来说，我们使用评判器来预测提示前缀的 Pass@k 成功概率，该概率源自一次 Pass@1 尝试。这个选择产生的学习信号比 Pass@1 更具选择性：它忽略容易解决的前缀，同时优先考虑那些成功概率仍然边缘化的困难前缀。我们表明，随着 $k$ 的增加，Pass@k 收敛到一个可达性指标，反映了一个前缀是否能够导致至少一个成功的后续序列。在显式状态图中，极限（$k \rightarrow \infty$）可以在 $O(|V|+|E|)$ 时间内计算，为直接信用分配提供了一个有前景的替代方法，无需采样对比轨迹。作为初步验证，SR-PPO 表现出稳定的学习动态，以及在数学推理基准（如 HMMT26 和 AIME24）上 Pass@128 成功率的持续提升。

## Abstract
Estimating token-level advantages in reinforcement learning (RL) for language models remains challenging because scaling up episodic experience collection is expensive. The difficulty intensifies for baseline advantage estimation methods, where repeated sampling causes trajectories to diverge into substantially different reasoning prefixes. In this context, RL algorithms such as GRPO prove limited: an outcome reward is too sparse to be attributed to specific actions like intermediate steps, and comparisons across sampled traces are non-trivial because they are heterogeneous. To mitigate both the computational cost of repeated sampling and the difficulty of credit assignment, we study single-rollout proximal policy optimization (SR-PPO) featuring token-level credit assignment in RL for language models. Instead of estimating advantages by normalizing episodic returns within the candidate group, we train a calibrated token-level credit critic using Monte Carlo outcomes from one rollout per prompt. Specifically, we use the critic to predict the Pass@k success probability at the prompt prefix, which is derived from a Pass@1 attempt. This choice yields a more selective learning signal than Pass@1: it discounts easily solved prefixes while prioritizing hard ones whose success probability remains marginal. We show that as $k$ increases, Pass@k converges to a reachability indicator, reflecting whether a prefix can lead to at least one successful continuation. In an explicit state graph, the limit ($k \rightarrow \infty$) can be computed in $O(|V|+|E|)$ time, offering a promising surrogate for direct credit assignment without the need to sample contrastive traces. As an initial validation, SR-PPO exhibits stable learning dynamics, along with consistent gains in Pass@128 success rates on mathematical reasoning benchmarks such as HMMT26 and AIME24.

---

## 论文详细总结（自动生成）

### 论文结构化中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题背景**：在语言模型（LM）的强化学习（RL）训练中，尤其是数学推理等任务，最终的正确性信号（outcome reward）过于稀疏，难以分配给每个中间token。现有的方法如GRPO通过组内比较多个完整轨迹来估计优势，但这种方法存在两个主要缺陷：一是重复采样成本高，尤其对于需要工具调用、记忆检索等长期智能体环境；二是多个rollout轨迹可能因推理路径不同（如调用不同工具）而高度异质，导致组内比较噪声大且不可靠。
- **核心挑战**：如何在**单次rollout**（每个prompt只采样一条轨迹）的条件下，实现有效的token级信用分配，避免组内归一化的高成本和统计噪声。这直接关系到长周期智能体训练的效率与可扩展性。
- **整体含义**：论文提出一种名为 SR-PPO（Single-Rollout Proximal Policy Optimization）的方法，通过训练一个“Pass@k”信用评判器（credit critic），从单次蒙特卡洛结果中学习token级别的优势估计。该方法旨在**用计算替代采样**，以低成本获得细粒度的训练信号。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：对于每个prompt的单条轨迹，训练一个前缀级信用模型（critic），预测当前部分解（prefix）在多次独立尝试下（Pass@k）至少有一次成功的概率。然后利用该值的前后变化（差分）作为token级优势信号。
- **关键技术细节**：
  1. **Pass@k值函数**：定义前缀状态 \( s_t \) 的Pass@k价值为 \( q_{\pi,k}(s_t) = 1 - (1 - q_{\pi}(s_t))^k \)，其中 \( q_{\pi}(s_t) \) 是单次成功的概率（Pass@1）。通过一个单独的神经网络参数化 \( \hat{q}_{\pi,k}(s_t) \)，训练时使用BCE和Brier score联合损失，监督信号为最终二值结果Y。
  2. **Token级优势设计**：优势函数定义为 \( A_{\pi,k}(s_t) = \Delta \hat{q}_{\pi,k}(s_t) + \lambda (Y - \hat{q}_{\pi,k}(s_T)) \)，其中 \( \Delta \hat{q}_{\pi,k}(s_t) = \hat{q}_{\pi,k}(s_t) - \hat{q}_{\pi,k}(s_{t-1}) \) 为局部信用差分，终端校正项使密集优势与最终结果对齐。
  3. **Pass@k的选择**：选择k>1（实验使用k=4），其导数在 \( q_{\pi} \) 接近1时趋于0，从而自动降低对已易解前缀的更新权重，将梯度集中于“硬但可达”的前缀，提高了学习信号的**选择性**。
  4. **单rollout策略梯度**：每步只用一个rollout，更新目标为 \( J(\theta) = \frac{1}{M} \sum_i \sum_t r_{i,t}(\theta) A_{i,t}^{(k)} \)，其中重要性权重 \( r_{i,t} \) 因为采用实时采样（θ=θ_old）而恒为1，等效于单步on-policy策略梯度。
  5. **可达性联系**：理论证明（Lemma A.1）当 \( k \to \infty \) 时，Pass@k收敛于二值可达性指标 \( r(s) = 1\{q_{\pi}(s) > 0\} \)。若状态图已知，可达性可通过O(|V|+|E|)的图遍历精确计算，为信用分配提供理论支撑。

#### 3. 实验设计：数据集、benchmark、对比方法

- **数据集与训练**：使用**DeepScaleR**数学推理数据集（来自HuggingFace）进行训练。模型基础为**Qwen3-1.7B**（thinking模式），最大生成长度9,182 tokens。
- **评估benchmark**：**AIME24、AIME25、HMMT26**（均为竞赛级数学推理基准）。评估时每个问题采样128次，计算Pass@k（k=1,2,4,8,16,32,64,128）。
- **对比方法**：
  - **GRPO**（Group Relative Policy Optimization）：使用8个rollout/组进行优势归一化，作为强基线。
  - **GAE-PPO**（Generalized Advantage Estimation with PPO）：标准TD引导的PPO，但只用一个rollout。
  - **SR-PPO变体**：Pass@1 SR-PPO、Pass@4 SR-PPO，以及不同梯度积累步数（g=1,2,4）的对照。

#### 4. 资源与算力
- 论文中**未明确说明**使用了何种GPU型号、数量以及训练总时长。仅提到使用了一个运行种子（single running seed）和有限的超参数选择。这可能表明实验规模较小，或者作者未详细披露。读者需注意这一信息缺失。

#### 5. 实验数量与充分性
- **实验组数**：主要包括：
  - 主验证曲线（图1b）：SR-PPO vs GRPO vs GAE-PPO 在Pass@8成功率上的对比（训练步骤150步）。
  - 格式分数（图2b）：Pass@8格式正确率。
  - 消融实验（图1c）：Pass@1 vs Pass@4 SR-PPO在不同梯度积累步数下的稳定性。
  - 跨benchmark泛化（图3）：在AIME24/25、HMMT26上随推理采样数的变化曲线。
  - Token级优势稀疏性分析（表1）：Pass@1与Pass@4的边际优势比例。
- **充分性评价**：
  - **优点**：实验覆盖了训练动态、多基准评估、消融和量化分析（稀疏性），逻辑链条完整。
  - **局限性**：仅使用单一种子、单一模型（1.7B）、单一数据集（DeepScaleR），未在多尺度模型或多个领域验证，也未报告方差或统计显著性测试。因此，结论的泛化性有待进一步验证。

#### 6. 论文的主要结论与发现
- 单rollout训练（SR-PPO）能够通过Pass@k信用模型实现有效的token级信用分配，避免组内归一化的高成本。
- Pass@4 SR-PPO在Pass@8和Pass@128指标上优于或接近GRPO（后者使用了8倍rollout成本），且训练动态更稳定。
- GAE-PPO在单rollout设置下基本失效，体现了TD引导在长距离数学推理中的缺陷。
- Pass@4相比于Pass@1具有更稀疏、更稳定的信用信号（表1），从而抑制了噪声更新，提升了训练稳定性。
- 实验表明，基于Pass@k的信用模型不仅提高了多采样性能（Pass@k随推理采样数线性增长），而且并未严重损害单样本性能（Pass@1），但在某些设置下Pass@1可能略逊于GRPO。

#### 7. 优点：方法或实验设计上的亮点
- **理论创新**：将信用分配与图可达性联系起来，提供了从概率值到二值指标的极限视角，并且证明了可计算性（O(|V|+|E|)）与估计误差的分离（Corollary B.1），具有理论指导意义。
- **实践创新**：Pass@k作为可调的超参数，自动调谐信用选择性，避免了手工设计进度奖励或过程奖励的需要。
- **实验设计**：在训练步数轴上对比时，特意控制了rollout总预算（x轴归一化），公平展示了SR-PPO的样本效率。
- **可视化**：附录提供token级优势的彩色可视化（附录C），直观展示了Pass@1与Pass@4信用分布的差异。

#### 8. 不足与局限
- **实验覆盖不足**：仅使用1.7B模型、单一种子、单一领域（数学推理），未涉及代码生成、工具调用等更复杂的智能体任务。
- **计算资源未报告**：缺乏GPU型号、训练时长等关键信息，影响可复现性评估。
- **消融不够全面**：未系统研究不同k值（如k=2,8,16）或不同λ的影响；未与最新方法（如TEMPO、TreePO）进行直接比较。
- **潜在偏差**：GRPO的调参可能未充分优化（仅调学习率），导致基线竞争力不足；Pass@1 SR-PPO的不稳定性可能通过更细致的超参数调整得到缓解。
- **应用限制**：为单rollout场景设计，但许多现有框架仍能批量生成，其优势在采样成本极高的场景（如长周期智能体）才能体现。

（完）
