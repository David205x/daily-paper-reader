---
title: Predictive Divergence Masks for LLM RL
title_zh: LLM强化学习中的预测性散度掩码
authors: "Xiangxin Zhou, Jiarui Yao, Penghui Qi, Bowen Ping, Jiaqi Tang, Haonan Wang, Tianyu Pang"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10848v1"
tags: ["query:rl"]
score: 9.0
evidence: 通过预测性散度掩码改进LLM的PPO强化学习
tldr: 大语言模型的强化学习通常使用信任区域掩码稳定更新，但PPO基于重要性比率的方向性标准是单样本代理，可能与散度变化符号不一致。本文提出预测性散度掩码，通过预测政策梯度步骤对散度的增减影响来替代比率标准，并针对top-K词汇表开发轻量级估计器。实验表明，该方法在不同模型规模下有效改进了RL训练，提高了方向判断的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10848v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1689, \"height\": 994, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1669, \"height\": 966, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1557, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1566, \"height\": 744, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1634, \"height\": 1037, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1631, \"height\": 1034, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1636, \"height\": 1031, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10848v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1633, \"height\": 1039, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10848v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1542, \"height\": 540, \"label\": \"Table\"}]"
motivation: PPO基于重要性比率的方向性标准与散度变化符号不一致，导致掩码失效。
method: 提出预测性散度掩码，在离散softmax政策下封闭形式预测梯度步骤对散度的影响，并用top-K估计器适配生产引擎。
result: 散度based方向比采样比率更好地对齐散度实际变化，提升RL训练效果。
conclusion: 预测性散度掩码能显著改进LLM的RL训练稳定性和效率。
---

## 摘要
大语言模型的强化学习通常依赖信任区域掩码来稳定离策略更新。主流的PPO风格方法使用采样令牌重要性比率满足两个准则：邻近性准则（检查策略是否偏离行为策略过远）和方向性准则（检查更新是否使策略进一步远离）。近期工作DPPO通过将PPO基于比率的测试替换为行为策略与训练策略之间的概率散度，改进了邻近性准则，但其方向性准则仍继承自PPO——仅当采样令牌重要性比率偏离1时才可能掩码令牌。我们观察到这种基于比率的方向性准则是单样本代理，其符号可能与定义邻近性准则的散度变化不一致。为此，我们提出预测性散度掩码，它询问下一步策略梯度步骤将增加还是减少与信任区域相同的散度。针对LLM强化学习中使用的离散softmax策略，我们推导出该预测的闭式解。由于生产部署引擎仅暴露词汇表的截断（Top-K）视图，我们开发了两种轻量级Top-K估计器用于该预测。详细分析表明，基于散度的方向比采样比率更能与散度的实际变化一致，由此产生的掩码在不同模型规模和精度设置下均能提升强化学习训练效果。

## Abstract
Reinforcement learning for large language models (LLMs) typically relies on trust-region masks to stabilize off-policy updates. The dominant PPO-style approach uses the sampled-token importance ratio for two criteria: a proximity criterion, which asks whether the policy has moved too far from the behavior policy, and a direction criterion, which asks whether the update pushes it farther away. Recent work DPPO improves the proximity criterion by replacing PPO's ratio-based test with a probability divergence between the behavior and training policies. However, its direction criterion is still inherited from PPO. A token can be masked only when the sampled-token importance ratio moves away from one. We observe that this ratio-based direction criterion is a single-sample proxy that can disagree in sign with the change of the divergence that defines the proximity criterion. We therefore propose the predictive divergence mask, which asks whether the next policy-gradient step will increase or decrease the same divergence used by the trust region. For the discrete softmax policies used in LLM RL, we derive this prediction in closed form. Because production rollout engines expose only a truncated (top-K) view of the vocabulary, we develop two lightweight top-$K$ estimators for this prediction. Detailed analysis shows the divergence-based direction is better aligned with the realized change of the divergence than the sampled ratio, and the resulting masks improve RL training across model scales and precision settings.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：LLM的RL训练通常采用PPO风格的信任区域掩码来稳定离策略更新。PPO使用采样令牌的**重要性比率**（`r_t = π(y_t|s_t)/μ(y_t|s_t)`）来同时判断两个准则：  
  - **邻近性准则**：`|r_t - 1| > ϵ`，判断策略是否偏离行为策略过远。  
  - **方向性准则**：`sign(Â_t (r_t - 1)) > 0`，判断更新是否使策略进一步远离行为策略。  
  近期工作DPPO将邻近性准则替换为基于完整分布散度的度量（如top-K KL散度），从而更准确地描述策略偏移。然而，DPPO的方向性准则仍沿用了PPO的基于单样本重要性比率的方法，这导致**方向性判断与散度实际变化可能存在符号不一致**——采样比率只观察到了一个令牌，忽略了softmax归一化耦合带来的全局影响。  
- **整体含义**：本文旨在解决这种不一致性，提出**预测性散度掩码**（Predictive Divergence Mask），让方向性准则也基于散度本身的一阶变化来决策，从而与基于散度的邻近性准则保持一致。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：对于超出信任区域（`D_t > δ`）的令牌，判断梯度更新是否会**增加**散度`D_t`（KL(μ∥π)）。若预测会增大，则掩码（梯度置零）；若预测会减小，则保持更新（即使`D_t > δ`）。  
- **关键技术细节**：  
  1. **散度方向导数的闭式推导**：对softmax策略，沿政策梯度方向（单位方向`v_i = 1[i=k] - π_i`，k为采样令牌）的KL散度导数（式7）：  
     `Ḋ = (π_k - μ_k) + Σ_i π_i (μ_i - π_i)`  
     其中第一项为局部项（基于采样令牌），第二项为全局耦合项（基于整个词汇表平均差距）。  
  2. **理解与比率方向准则的关系**：`sign(π_k - μ_k) = sign(r - 1)`，所以PPO的方向准则只使用了局部项，忽略了全局项。当全局项与局部项符号相反且幅度足够大时，两种准则产生分歧。  
  3. **针对Top-K截断词汇的轻量级估计**：生产引擎只暴露Top-K概率及尾部质量。提出两种估计器：  
     - **聚合尾估计器**（`Ḋ_agg`）：将尾部分配给一个聚合令牌。  
     - **均匀尾估计器**（`Ḋ_uni`）：将尾部质量均匀分配到所有未见令牌（分母n-m很大，尾部贡献很小）。  
     两者差异很小（式12），且计算开销仅需对已知的top-K概率和尾部质量做简单运算，无需额外前向/反向传播。  
  4. **最终掩码定义**（式13）：  
     ```
     M_pred_t = 0  if sign(Â_t · Ḋ_t) > 0 and D_t > δ,
                1  otherwise.
     ```
     梯度更新公式与DPPO-TopK-KL相同，仅方向准则替换为散度的一阶变化符号。

### 3. 实验设计：数据集、评估基准、对比方法

- **训练数据集**：DAPO-Math-17k的过滤版本（约13k样本）。  
- **评估基准**：AIME 2024 和 AIME 2025，每个问题采样16条回答，报告平均准确率（avg@16）。  
- **模型**：Qwen3-4B-Base、Qwen3-8B-Base、Qwen3-30B-A3B-Base（混合专家模型）。  
- **对比方法**：  
  - GRPO Clip-Higher（ϵ_low=0.2, ϵ_high=0.28）作为比率裁剪基线。  
  - DPPO-TopK-KL（主要基线）——使用top-K KL邻近性准则 + PPO的比率方向准则。  
  - DPPO-TopK-KL + 预测性散度掩码（聚合尾估计器）。  
  - DPPO-TopK-KL + 预测性散度掩码（均匀尾估计器）。  
- **配置**：K=20，默认δ=0.15；额外实验δ=0.05作为敏感度测试。  
- **额外分析**：在61个随机种子上进行令牌级方向准则单独对比，考察保留但散度实际增大的比例（unsafe-keep rate）以及保留且散度收缩的比例。

### 4. 资源与算力

- **明确说明**：实验基于 4 个节点，每个节点 8 块 NVIDIA H20 GPU，共 32 块 GPU。  
- **未明确说明**：训练时长、总GPU小时数、各方法的训练步数（图中显示最高约400步，但未记录消耗时间）。  
- **精度设置**：除默认BF16外，针对Qwen3-30B-A3B-Base设置了两种FP8低精度场景（仅推理FP8或全流程FP8），作为数值不一致的压力测试。

### 5. 实验数量与充分性

- **实验数量**：  
  - **主实验**（δ=0.15）：4个模型/精度设置 × 4种方法 = 16条训练曲线（图1）。  
  - **紧阈值实验**（δ=0.05）：相同设置，仅使用聚合尾估计器（图2）。  
  - **令牌级分析**：61个独立种子，每个种子1024条序列。统计约1435个超出信任区域的令牌，其中约82个令牌产生分歧（两个方向准则不同），重点分析这些分歧令牌。  
  - **消融实验**：通过两种尾估计器对比验证对尾模型不敏感。  
- **充分性评价**：  
  - **优点**：覆盖了不同模型规模（4B、8B、30B MoE）和不同精度（BF16、FP8），且包含两种精度场景的消融。令牌级分析直接验证了方向准则与散度实际变化的一致性，提供了机制证据。  
  - **不足**：  
    - 测试任务仅限于数学推理（AIME），未覆盖对话、指令遵循或一般知识任务。  
    - 基线仅包含GRPO和DPPO，未与更近期的后续工作（如使用散度正则化的方法）对比。  
    - 仅报告了avg@16指标，缺乏更全面的度量（如pass@1, pass@k, 奖励曲线稳定性等）。  
    - 紧阈值实验（δ=0.05）虽表明预测性掩码仍优于DPPO，但所有方法性能均下降，未深入分析为何紧阈值不利。

### 6. 论文的主要结论与发现

1. **方向准则的一致性**：基于散度的方向准则（预测性散度掩码）与散度实际变化的对齐程度优于基于采样比率的准则：在分歧令牌上，预测性掩码的“不安全保持率”（保持更新但散度增大）比比率掩码低约2.7个百分点（34.2% vs 36.9%）。  
2. **训练提升**：在所有模型/精度设置下，预测性散度掩码在AIME24/25上的平均准确率优于DPPO-TopK-KL，而GRPO在30B MoE下出现训练崩溃。  
3. **尾估计器的鲁棒性**：聚合尾与均匀尾估计器表现几乎相同，说明对尾部建模方式不敏感。  
4. **紧阈值的鲁棒性**：在δ=0.05的更严格信任区域内，预测性掩码仍稳定优于DPPO，表明其方向准则在更保守设定下也更有效。  
5. **理论贡献**：推导了KL散度沿政策梯度方向的闭式导数，并揭示了PPO方向准则相当于只取局部项而忽略全局softmax耦合项。

### 7. 优点：方法或实验设计上的亮点

- **理论与实际紧密结合**：从数学推导到生产引擎限制的适配完整一致，方向准则的设计基于散度的方向导数，无需额外超参数。  
- **低计算开销**：两种估计器仅需对已有的top-K概率做简单数学运算，无额外前向/反向传播或有限差分。  
- **清晰的消融实验**：通过令牌级分析直接比较两种方向准则对真实散度变化的影响，实验设计干净，控制变量严格（仅方向不同，其余完全一致）。  
- **覆盖实际部署场景**：包括FP8低精度训练，模拟了rollout与训练引擎数值不一致的真实情况，实验结果更具说服力。  
- **可解释性**：分解局部项与全局项，清晰地解释了PPO方向准则的缺陷来源。

### 8. 不足与局限

- **实验覆盖有限**：仅使用数学推理任务（AIME），未在其他类型任务（如通用对话、代码生成、安全对齐）上验证泛化性。  
- **对比方法不全面**：未与近期提出的散度正则化方法（如Ratio-Variance Regularization、Divergence Regularization）进行比较，这些方法也可能受益于更好的方向准则。  
- **令牌级分析的局限性**：方向准则只预测单个令牌梯度对散度的局部一阶变化，但实际参数更新聚合了批次中所有令牌的梯度，会产生跨令牌的相互作用。论文承认这种局部近似，但未量化其误差或提供补偿机制。  
- **阈值敏感度分析不足**：仅测试了δ=0.15和0.05两个值，未系统扫描其他δ值对各类方法的影响。  
- **缺乏消融：** 未实验“仅替换方向准则而不改变邻近性准则”的更严格消融（DPPO本身已使用散度邻近性，本文实质上就是这种消融，但可以更明确地说明）。  
- **计算资源细节缺失**：未报告训练时间、收敛步数对比等与效率相关的指标。  
- **潜在偏差风险**：实验均基于Qwen3系列模型，可能在其他架构（如DeepSeek、LLaMA）上表现不同；训练数据仅来自数学领域，偏差可能存在于针对数学问题的散度行为。

（完）
