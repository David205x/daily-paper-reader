---
title: Interpretable Uncertainty for Adaptive Retrieval and Reasoning in Question Answering
title_zh: 用于问答中自适应检索与推理的可解释不确定性
authors: "Ritajit Dey, Iadh Ounis, Graham McDonald"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07380v1"
tags: ["query:rl"]
score: 8.0
evidence: 提出基于不确定性感知的自适应检索用于问答RAG
tldr: 大型语言模型在问答中易产生幻觉，检索增强生成（RAG）可提升事实性，但现有检索策略不透明或计算成本高。本文提出基于模型内部表示的不确定性感知框架，通过单次前向传播估计知识不足与歧义/冲突信号，自适应触发RAG或额外推理。该方法在保持性能的同时提升了决策透明度，为可解释的用户工具提供了实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07380v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 885, \"height\": 441, \"label\": \"Figure\"}]"
motivation: 现有RAG策略依赖不透明规则或多步提示，缺乏效率与可解释性，需要基于显式信号的自适应方法。
method: 从LLM隐藏状态中分解估计知识不足和歧义/冲突两类不确定性，单次前向传播完成信号提取。
result: 不确定性信号指导系统在知识不足时触发RAG，在歧义/冲突高时启用额外推理流程。
conclusion: 该框架提供了透明高效的替代方案，支持可解释的检索与推理自适应决策。
---

## 摘要
大型语言模型（LLM）在问答（QA）中表现出强大的性能，但仍然容易产生幻觉，且透明度有限。检索增强生成（RAG）可以提高事实性，但何时以及如何从外部资源进行检索的决策通常基于不透明的策略或计算效率低下的多步提示程序。我们提出了一种基于不确定性感知的自适应问答框架，该框架利用从LLM内部表示中提取的明确信号。我们区分了知识不足与知识模糊或冲突，并通过单次前向传播从隐状态中高效地估计这些信号。这些信号指导系统行为：当知识不足时触发RAG，而当模糊或冲突较高时应用额外的推理。通过将自适应决策建立在可分解且高效估计的不确定性信号之上，该方法为现有的检索和推理策略提供了一种透明且实用的替代方案，支持可解释的用户面向工具的设计。

## Abstract
Large language models (LLMs) achieve a strong performance in question answering (QA), but remain prone to hallucinations and suffer from limited transparency. Retrieval-augmented generation (RAG) can improve factuality, yet decisions about when and how to retrieve from external resources are typically based on opaque policies or computationally inefficient multi-step prompting procedures. We propose an uncertainty-aware framework for adaptive QA based on explicit signals derived from LLM internal representations. We distinguish between knowledge insufficiency and knowledge ambiguity or conflict, and efficiently estimate these from hidden states in a single forward pass. These signals guide system behaviour: RAG is triggered when knowledge is insufficient, while additional reasoning is applied when ambiguity or conflict is high. By grounding adaptive decisions in decomposed and efficiently estimable uncertainty signals, this approach provides a transparent and practical alternative to existing retrieval and reasoning strategies supporting the design of interpretable user-facing tools.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：大型语言模型（LLM）在问答任务中表现优异，但容易产生幻觉、依赖过时知识、缺乏透明度。检索增强生成（RAG）能提升事实性，但现有自适应检索策略要么依赖不透明的隐式决策策略（如Self-RAG、Adaptive-RAG），要么依赖计算成本高的多步提示机制，难以提供可解释的用户反馈。
- **动机**：需要一种既能区分不同来源的不确定性（知识不足 vs. 歧义/冲突）、又能高效估计并用于自适应决策的方法，从而在保持性能的同时提高决策透明度和可解释性。
- **整体含义**：本文提出将不确定性分解为两种可解释信号，并基于它们驱动不同的系统行为（检索或推理），为构建用户可理解的问答系统提供新范式。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用LLM的隐藏状态，通过轻量级回归探针（regression probes）在单次前向传播中估计两种不确定性信号，然后基于阈值触发不同干预措施。
- **关键技术细节**：
  - **知识不足（Knowledge Insufficiency）**：定义为目标事实在预训练语料中的出现次数（越低表示越不确定）。训练回归探针从LLM隐藏状态预测该次数。
  - **知识歧义/冲突（Knowledge Ambiguity/Conflict）**：定义为预训练语料中不同版本事实出现次数的熵（越高表示越不确定）。同样训练回归探针估计该熵值。
  - **自适应决策机制**：当知识不足超过阈值时触发RAG；当歧义/冲突超过阈值时启用额外推理（如链式思维、自一致性）；两者都低时直接生成答案。
  - **特点**：所有估计仅在单次前向传播中完成，无需多次采样或额外模型，高效且适合多步/智能体场景。

## 3. 实验设计：数据集、benchmark、对比方法

- **数据集**：Natural Questions（NQ）数据集（事实型问答）。
- **基准模型**：Llama-2-7b-chat。
- **对比方法**：
  - LLM-only基线（无检索）
  - 始终开启RAG基线（always-on RAG）
  - 两种不确定性估计指标：Semantic Entropy (SE) 和 Weighted Entropy Production Rate (WEPR) 分别用于触发RAG。
- **评估**：使用McNemar检验比较性能差异。结果显示本文方法在两种指标下均显著优于两个基线（SE触发：+5.9% vs LLM-only，+3.3% vs always-on RAG；WEPR触发：+4.7% vs LLM-only，+2.1% vs always-on RAG）。

## 4. 资源与算力

- **文中未明确说明**：论文未提及使用的GPU型号、数量、训练时长、推理耗时等资源信息。仅提到在NQ数据集上使用Llama-2-7b-chat模型，但未提供训练回归探针的具体计算成本或实验环境细节。

## 5. 实验数量与充分性

- **实验数量**：极简。只报告了一组初步实验（NQ数据集，单一模型，两种不确定性指标触发RAG），未进行消融实验（如不同阈值的影响、不同不确定性估计方法的对比、多数据集验证、推理方法对比等）。
- **充分性与公平性**：实验不充分。仅给出两个基线对比，缺乏与最新自适应RAG方法（如Self-RAG、Adaptive-RAG）的直接比较，也未评估在歧义/冲突高时触发推理的效果（本文只做了RAG触发实验）。论文本身是短文（工作坊论文），属于初步探索，实验覆盖范围有限。

## 6. 论文的主要结论与发现

- 基于隐藏状态的不确定性估计可以区分知识不足和歧义/冲突，并用于自适应决策。
- 在NQ数据集上，不确定性驱动的自适应RAG（无论使用SE还是WEPR）优于一直检索或从不检索的基线，表明该方向有潜力。
- 该方法兼顾了可解释性与效率（单次前向传播），为透明用户交互工具提供了基础。

## 7. 优点

- **创新性**：明确区分两种不确定性来源，并分别对应不同干预措施，比现有方法更细粒度。
- **可解释性**：决策基于显式信号（知识不足、歧义/冲突），而非黑盒策略，有利于用户理解和系统审计。
- **高效性**：利用单次前向传播的隐藏状态估计，避免多次采样或辅助模型，适合实际部署。
- **设计理念**：将可解释性作为设计原则而非事后解释，提升系统信任度。

## 8. 不足与局限

- **实验验证薄弱**：仅在一个数据集（NQ）和一个模型（Llama-2-7b-chat）上进行了初步实验，缺乏多模型、多领域验证；未对比最先进的自适应RAG方法（如Self-RAG）。
- **未验证歧义/冲突触发推理的效果**：论文只展示了知识不足触发RAG的实验，未实际实现和评估歧义/冲突高时触发额外推理的场景。
- **不确定性估计未正式实现**：文中提出的回归探针方法目前只是概念性描述，未给出训练细节、探针结构、数据构造方式，也未报告实际估计精度。
- **阈值依赖**：决策机制基于预设阈值，但未讨论如何设定阈值及其敏感性。
- **计算资源空缺**：未提供任何关于训练探针或推理的计算成本数据，难以评估实际效率优势。
- **通用性存疑**：假设隐藏状态能有效编码知识不足和歧义/冲突，但该假设需要更多证据支持（例如不同模型架构、规模下的鲁棒性）。

（完）
