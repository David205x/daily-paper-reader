---
title: "Information Gain-based Rollout Policy Optimization: An Adaptive Tree-Structured Rollout Approach for Multi-Turn LLM Agents"
title_zh: 基于信息增益的展开策略优化：一种面向多轮LLM智能体的自适应树结构展开方法
authors: "Yijun Zhang, Fan Xu, Jiaxin Ding, Yule Xie, Shiqing Gao, Xin Ding, Haoxiang Zhang, Luoyi Fu, Xinbing Wang"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06223v1"
tags: ["query:rl"]
score: 9.0
evidence: 基于信息增益的策略优化用于多步LLM推理
tldr: 多轮LLM代理在长程搜索任务（如搜索增强问答）中需做出序列中间决策，现有强化学习方法展开预算分配未评估中间状态效用，导致低价值状态浪费大量计算资源。本文提出信息增益引导的展开策略优化（IGRPO），根据节点信息增益动态分配预算，优先扩展高价值分支并逐步抑制无望分支，同时诱导出显式教师分布作为策略优化目标，统一自适应探索与策略学习。在七个搜索增强QA基准上，IGRPO在相同预算下始终优于强基线，验证了方法的有效性，为长程任务提供了高效且可解释的预算分配机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06223v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1378, \"height\": 366, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06223v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1385, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06223v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 568, \"height\": 362, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06223v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 544, \"height\": 346, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06223v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 521, \"height\": 522, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06223v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1386, \"height\": 648, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06223v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1423, \"height\": 884, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06223v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1001, \"height\": 755, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06223v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1376, \"height\": 691, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06223v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1447, \"height\": 717, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06223v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1444, \"height\": 239, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06223v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1443, \"height\": 216, \"label\": \"Table\"}]"
motivation: 现有方法展开预算分配未评估中间状态效用，导致低价值状态浪费计算资源。
method: IGRPO以节点级信息增益分配展开预算，自适应扩展信息丰富分支，并诱导教师分布指导策略优化。
result: 在七个搜索增强QA基准上，IGRPO在预算约束下一致优于强基线。
conclusion: 信息增益引导的预算分配与教师分布策略优化有效提升长程搜索代理性能。
---

## 摘要
强化学习已成为改进大型语言模型（LLM）智能体在长视野搜索任务上的有前景范式，其中智能体必须在获得最终结果之前做出一系列中间决策。然而，现有方法仍面临一个关键限制：展开预算的分配往往没有明确评估中间状态的效用。因此，即使在信息量上不同分支可能有巨大差异，大量计算也可能花费在低价值状态上。在本文中，我们提出了基于信息增益的展开策略优化（IGRPO），这是一种将中间状态信息量作为展开收集组织原则的策略优化框架。具体来说，IGRPO通过根据节点级信息量分配扩展预算来执行预算感知的树结构展开，从而信息更丰富的分支被更频繁地展开，而前景不佳的分支则被逐步抑制。我们进一步证明，基于信息增益的展开在轨迹上诱导出一个显式的限制性教师分布，这自然产生了清晰的策略优化目标，从而在单一框架内统一了自适应树结构探索与原则性策略学习。在七个具有挑战性的搜索增强型QA基准上的实验表明，在相同的展开预算约束下，IGRPO持续优于强基线，验证了利用诱导的教师分布来指导长视野搜索智能体策略优化的有效性。

## Abstract
Reinforcement learning has become a promising paradigm for improving large language model (LLM) agents on long-horizon search tasks, where the agent must make a sequence of intermediate decisions before receiving a final outcome. However, existing methods still face a key limitation: the rollout budget is often allocated without explicitly assessing the utility of intermediate states. As a result, substantial computation may be spent on low-value states, even though different branches can vary drastically in their informativeness. In this paper, we propose Information Gain-based Rollout Policy Optimization (IGRPO), a policy optimization framework that treats intermediate-state informativeness as the organizing principle of rollout collection. Specifically, IGRPO performs budget-aware tree-structured rollouts by allocating expansion budget according to node-level informativeness, so that more informative branches are expanded more frequently while unpromising branches are progressively suppressed. We further demonstrate that the information gain-based rollout induces an explicit limiting teacher distribution over trajectories, which naturally yields a clear policy optimization target, thereby unifying adaptive tree-structured exploration with principled policy learning under a single framework. Experiments on seven challenging search-augmented QA benchmarks demonstrate that IGRPO consistently outperforms strong baselines under the same rollout budget constraints, validating the effectiveness of leveraging the induced teacher distribution to guide policy optimization for long-horizon search agents.

---

## 论文详细总结（自动生成）

# 基于信息增益的展开策略优化（IGRPO）论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：多轮LLM智能体在长程搜索任务（如搜索增强问答）中，需要在与外部工具多次交互后产生最终答案。现有强化学习方法（如GRPO、树搜索方法）在生成展开轨迹时，通常**不加区分地分配展开预算**，没有评估中间状态的效用。这导致大量计算浪费在低价值甚至误导性的中间节点上，而信息丰富的分支却可能探索不足。
- **整体意义**：作者提出中间状态的“信息增益”应作为组织展开收集的核心原则，通过**预算感知的树结构展开**，将更多计算分配给有希望的分支，同时逐步抑制无望分支。这不仅提升了计算效率，还自然诱导出一个“教师分布”，为策略优化提供清晰的优化目标，从而统一自适应探索与原则性策略学习。

## 2. 方法论

### 核心思想
- **信息增益（Information Gain, IG）**：定义为到达中间节点后，当前策略对正确答案的归一化似然的边际提升。即 \( \text{IG}(h) = s_\theta(h; a) - s_\theta(\text{parent}(h); a) \)，其中 \( s_\theta(h; a) = \pi_\theta(a \mid h) \) 是条件于历史 \( h \) 的答案似然。
- **节点级信息度（informativeness）**：论文采用 \( \text{val}(h) = \frac{s_\theta(h; a) + \text{IG}(h)}{2} \)，兼顾当前承诺与最新进展。
- **预算感知树展开**：在每一层扩展时，根据节点信息度分配扩展概率 \( p_i(h) \propto \exp(\gamma \cdot \text{val}(h)) \)，其中 \( \gamma \) 控制偏置强度。然后独立采样 \( B_i \) 个节点进行扩展，形成下一层。低信息度节点被选中的概率低，从而减少其消耗的预算。

### 关键技术细节
- **教师分布的理论分析**：定理1证明，当每层预算足够大时，采样到的完整轨迹的分布收敛于 \( \mu_\theta(o) \propto \pi_\theta(o) \exp(\gamma V(o)) \)，其中 \( V(o) \) 是轨迹累积信息度。定理2进一步证明该分布是变分目标 \( \max_\mu \mathbb{E}_\mu[V] - \frac{1}{\gamma} D_{\text{KL}}(\mu \| \pi_\theta) \) 的唯一解。
- **策略优化目标**：基于诱导的教师分布 \( \mu_{\theta_{\text{old}}} \) 采样轨迹，采用类似GRPO的组相对优势优化：
  \[
  J_{\text{IGRPO}}(\theta) = \mathbb{E}_{(q,a),\{o_i\}\sim \mu_{\theta_{\text{old}}}} \left[ \frac{1}{N} \sum_{i=1}^N \frac{1}{T_i} \sum_{t=1}^{T_i} \sum_{k=1}^{|\tau_{i,t}|} \min(r_{i,t,k} \hat{A}_i, \text{clip}(r_{i,t,k}, 1-\epsilon, 1+\epsilon)\hat{A}_i) - \beta D_{\text{KL}}(\pi_\theta \| \pi_{\text{ref}}) \right]
  \]
  其中 \( \hat{A}_i \) 是组相对优势，\( r_{i,t,k} \) 是重要性采样比。
- **算法流程**：对于每个训练迭代，固定旧策略，构建搜索树（根为问题），依次对每层计算各节点信息度，按概率分配预算扩展，记录完整轨迹（包括因无法扩展而终止的路径）。然后使用收集到的轨迹执行组策略优化。注意，未完整展开的路径使用其累积信息增益作为奖励（乘以系数0.5，后期切换为全值）。

## 3. 实验设计

### 数据集与 benchmark
- **单跳QA**：NQ（Natural Questions）、TriviaQA、PopQA。
- **多跳QA**：HotpotQA、2WikiMultiHopQA、MuSiQue、Bamboogle。
- 评价指标：精确匹配（Exact Match, EM）。
- 训练数据：混合NQ和HotpotQA。

### 对比方法
- 未训练的基线：R1-Instruct（指令微调骨干）。
- 链式搜索强化学习方法：Search-R1、ZeroSearch、StepSearch、GiGPO、IGPO。
- 树式搜索强化学习方法：Tree-GRPO。
- 所有方法使用相同的骨干模型（Qwen2.5-3B-Instruct 和 Qwen2.5-7B-Instruct）、相同的检索器（E5）、相同的最大交互轮数（4）、相同的训练迭代数（200）。

## 4. 资源与算力

- **GPU型号与数量**：
  - Qwen2.5-3B：4× H20 GPU。
  - Qwen2.5-7B：8× H20 GPU。
- **训练时长**：论文未明确给出具体训练小时数，仅说明训练了200个iterations。批量大小256，最大提示长度4096，最大响应长度512。
- **其他**：使用E5作为检索器，返回top-3结果。训练时使用规则奖励（正确=1，错误=0，无效动作-0.01）。KL正则化系数0.001，学习率1e-6。

## 5. 实验数量与充分性

- **主实验**：在7个数据集上比较了3B和7B两种尺度下的9种方法（含IGRPO），共14个条件（7数据集×2尺度），但部分基线在StepSearch上只报告了部分数据集（可能因为原文仅报告了选定数据），其他方法均完整报告。
- **消融实验**：
  - 温度参数 \( \gamma \) 的消融（0, 1, 5），显示 \( \gamma=1 \) 最佳。
  - 每节点最大扩展预算减少对熵和性能的影响（图7）。
  - 每阶段预算缩减实验（IGRPO-low），性能仅下降0.9%，仍优于所有基线。
  - 奖励设计消融（四种累积信息增益映射方式），找到最佳设置（前期0.5倍，后期全值）。
- **额外分析**：
  - 训练动态熵损失比较（IGPO vs Tree-GRPO vs IGRPO）。
  - 推理时工具调用次数对比（IGRPO使用更少工具调用）。
  - 案例研究：展示了树结构展开中信息度如何引导预算分配。
- **充分性评价**：实验设计较为全面，覆盖了单跳、多跳，对比了链式和树式方法，消融了关键超参数和设计选择。但未报告多次独立重复的统计显著性（如标准差），也未在更多任务类型（如代码、游戏）或更大模型（>7B）上验证。整体而言，实验较充分，结论可靠，但统计严谨性略有不足。

## 6. 主要结论与发现

- **性能提升**：IGRPO在3B和7B骨干上平均分别达到45.4%和48.2% EM，在所有数据集上均优于或持平最强基线。3B上平均提升3.1%，7B上提升0.9%。
- **优势来源**：信息增益引导的预算分配使策略更聚焦于有希望的状态，产生更高效的搜索行为；诱导的教师分布提供了清晰的优化目标，避免了盲目探索。
- **熵调节**：IGRPO在训练中保持适度熵，不像IGPO过早坍缩、也不像Tree-GRPO熵过高导致收敛困难。
- **工具调用效率**：IGRPO在推理时平均工具调用次数更少，表明学会了更选择性地搜索。

## 7. 优点

1. **理论上有据可依**：证明了信息增益展开诱导的教师分布是变分问题的最优解，为策略优化提供了明确目标。
2. **自适应预算分配**：不是硬阈值而是概率分配，既优先高价值节点又保留探索机会，整体更鲁棒。
3. **统一框架**：将探索（树展开）与利用（策略优化）合并为单一框架，简洁有效。
4. **实验覆盖全面**：在7个不同难度的基准上验证，消融实验涵盖了关键设计要素。
5. **效率提升**：推理时工具调用减少，说明方法学到的策略更经济。

## 8. 不足与局限

1. **对信号定义的依赖**：信息增益需要计算给定中间历史下正确答案的似然，这要求训练时能访问真实答案（ground-truth）。对于没有明确答案或答案格式不一致的任务难以套用。
2. **适用性局限**：目前仅在搜索增强QA上评估，未在更广泛的任务（如工具使用、环境交互、数学推理）上验证。
3. **计算开销**：树展开需要为每个活跃节点计算信息增益（需前向模型计算答案似然），这带来了额外训练成本。论文未详细分析计算开销。
4. **实验统计严谨性**：未报告多次独立运行的标准差或置信区间，可能降低结果的可复现信心。
5. **超参数敏感**：温度 \( \gamma \)、奖励设计中的缩放系数（0.5）等需要手动调节，不同任务可能需调整。
6. **最大交互轮数固定**：论文设置最大交互轮数为4，对于需要更多轮探索的复杂任务可能受限。

（完）
