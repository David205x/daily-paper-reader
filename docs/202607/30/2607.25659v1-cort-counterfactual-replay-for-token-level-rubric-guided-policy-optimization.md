---
title: "CoRT: Counterfactual Replay for Token-Level Rubric-Guided Policy Optimization"
title_zh: CoRT：用于令牌级准则引导策略优化的反事实重放
authors: "Bo-Wen Zhang, Junwei He, Wen Wang, Song-Lin Lv, Wentao Ma, Rongyi Lin, Shuhan Zhong, Lan-Zhe Guo"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25659v1"
tags: ["query:rl"]
score: 9.0
evidence: 面向大语言模型的基于评分标准的强化学习中令牌级别信用分配方法
tldr: 模型训练中，基于评分标准的强化学习将结构化评价简化为标量奖励，导致无法细粒度分配信用。为此提出CoRT，通过反事实重放对比原始与无标准提示下的词元对数似然，生成词元级权重来重新分配GRPO优势信号，无需额外评分模型。实验表明，CoRT在多数对比中优于响应级GRPO，平均提升4.4百分点，且保持GRPO的简洁稳定性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25659v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 802, \"height\": 726, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25659v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1649, \"height\": 801, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25659v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 743, \"height\": 753, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25659v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 712, \"height\": 724, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25659v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 796, \"height\": 814, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25659v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1855, \"height\": 1155, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25659v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 902, \"height\": 390, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25659v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 901, \"height\": 362, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25659v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 884, \"height\": 388, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25659v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 872, \"height\": 559, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25659v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 871, \"height\": 352, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25659v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 873, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25659v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 869, \"height\": 498, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25659v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 868, \"height\": 567, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25659v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 840, \"height\": 542, \"label\": \"Table\"}]"
motivation: GRPO管道中将评分标准降为响应级奖励并均匀广播，缺乏对词元级信用分配的显式机制。
method: CoRT使用反事实重放对比同一响应在有/无评分标准提示下的词元对数似然，映射为归一化权重重新分配优势信号。
result: 在多个指令微调模型和奖励粒度上，CoRT相比响应级GRPO平均提升4.4个百分点，与学习型词元信用基线相当。
conclusion: 策略内部的反事实似然对比可有效实现词元级信用分配，同时保留GRPO的简单性和稳定性。
---

## 摘要
基于准则的强化学习通过依据显式标准评估模型输出来丰富语言模型训练。然而，在GRPO风格的流程中，这些结构化判断被简化为标量的响应级奖励，并转换为响应级优势，然后均匀地分配给所有生成的令牌。这导致即使在不同的标准基于不同的片段、格式决策或语义选择时，响应内也没有明确的信用分配机制。我们提出CoRT，一种用于准则条件GRPO的令牌级信用加权方法。CoRT不训练辅助的令牌评分模型，而是使用反事实重放在原始准则条件提示和匹配的无准则提示下重新评分相同的采样响应。由此产生的令牌级对数似然对比作为对准则上下文依赖的代理。CoRT将这些对比映射到有界、响应归一化的权重，并使用它们将带符号的GRPO优势重新分配到令牌之间，而不引入辅助评分器或改变响应级奖励。在指令微调模型和奖励粒度上的实验表明，CoRT在绝大多数比较中优于匹配的响应级GRPO，平均增益为4.4个百分点。该方法与学习的令牌级信用基线保持竞争力，同时避免了单独的相关性学习阶段。这些结果表明，策略内部的反事实似然对比为响应内信用分配提供了有效的训练信号，同时保留了GRPO的简单性和稳定性。

## Abstract
Rubric-based reinforcement learning enriches language model training by evaluating model outputs against explicit criteria. Yet in GRPO-style pipelines, these structured judgments are reduced to a scalar response-level reward and converted into a response-level advantage, which is broadcast uniformly to all generated tokens. This leaves no explicit mechanism for allocating credit within a response, even when different criteria are grounded in different spans, formatting decisions, or semantic choices. We propose CoRT, a token-level credit weighting method for rubric-conditioned GRPO. Instead of training an auxiliary token scoring model, CoRT uses counterfactual replay to rescore the same sampled response under the original rubric-conditioned prompt and a matched criteria-free prompt. The resulting tokenwise log-likelihood contrasts serve as a proxy for dependence on the rubric context. CoRT maps these contrasts to bounded, response-normalized weights and uses them to redistribute the signed GRPO advantage across tokens, without introducing an auxiliary scorer or changing the response-level reward. Experiments across instruction-tuned models and reward granularities show that CoRT improves over matched response-level GRPO in the vast majority of comparisons, with an average gain of 4.4 percentage points. The method remains competitive with learned token-level credit baselines while avoiding a separate relevance-learning stage. These results suggest that policy-internal counterfactual likelihood contrasts provide an effective training signal for within-response credit allocation while retaining the simplicity and stability of GRPO.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将基于您提供的论文内容，对该论文进行结构化、深入、客观的总结。

### **论文核心总结：CoRT：用于令牌级准则引导策略优化的反事实重放**

#### 1. 核心问题与整体含义（研究动机和背景）

*   **研究动机**：当前基于评分标准（Rubric）的强化学习（RL）在训练大语言模型（LLM）时，通常会将结构化的、针对多个维度的评估（如正确性、格式、安全性）简化为一个单一的、响应级的标量奖励。在流行的GRPO（Group Relative Policy Optimization）等策略优化方法中，这个响应级奖励被进一步转换为一个响应级优势（Advantage），并均匀地（uniformly）“广播”给生成响应中的所有词元（token）。这种处理方式忽略了不同词元在满足不同准则时贡献度不同的内在差异，导致缺乏响应内部细粒度的信用分配（credit assignment）机制。
*   **整体含义**：论文旨在解决上述“结构化准则”与“均匀化分配”之间的错配问题。作者提出，可以利用从策略模型本身（policy-internal）提取的信号，在不引入额外辅助模型的前提下，实现词元级的信用分配，从而更有效地利用评分标准中的结构化信息来指导模型训练，提升指令遵循能力。

#### 2. 提出的方法论：CoRT

*   **核心思想**：CoRT的核心是**反事实重放（Counterfactual Replay）**。对于一个已经采样好的完整响应，CoRT会分别在有评分标准提示（原文 `x+`）和无评分标准提示（反事实提示 `x-`）两种情况下，重新评估每个词元的对数似然概率。两种情境下的似然差异，可以作为该词元对评分标准依赖程度的一个有效代理（proxy）。这种似然差异越大，表明该词元越“专门”为满足特定评分标准而生成。
*   **关键技术细节与流程**（对应论文Algorithm 1）：
    1.  **标准GRPO流程**：对于一组（group）提示（包含评分标准 `x+`），生成 `G` 个响应 `yi`，并通过准则验证器（rubric verifier）得到每个响应的标量奖励 `ri`，进而计算出GRPO优势 `Ai`。
    2.  **反事实评分**：保持已生成的响应词元序列 `yi` 不变，将其输入到无准则版本的提示 `x-` 下，计算并记录每个词元 `yi,t` 在此反事实条件下的对数似然 `ℓ⁻ᵢ,ₜ`。同时，在原始标准提示下的对数似然 `ℓ⁺ᵢ,ₜ` 已在正常生成阶段获得。
    3.  **生成对比度**：计算每个词元的反事实对比度 `Δᵢ,ₜ = ℓ⁺ᵢ,ₜ - ℓ⁻ᵢ,ₜ`。该值越大，表示词元对准则的依赖越强。
    4.  **权重映射与归一化**：
        *   将无界对比度 `Δᵢ,ₜ` 通过一个带中心偏移 `b` 和温度系数 `τ` 的 `sigmoid` 函数映射到有界范围 (-0.5, 0.5) 内的分数 `sᵢ,ₜ`。
        *   通过一个缩放系数 `η` 和训练步数决定的调度系数 `λₖ` 得到临时权重 `˜wᵢ,ₜ`，使其范围可控在 `(1 - ηλₖ/2, 1 + ηλₖ/2)`。
        *   **响应内归一化**：为防止权重偏移导致整体更新量改变，论文强制要求每个响应内的平均权重为1，从而保证了整体优势信号的量级不变。
    5.  **调度激活**：论文使用一个基于训练步数的立方Hermite SmoothStep调度器来控制 `λₖ`，在训练初期 `λₖ≈0`，使得算法行为接近原始GRPO；随着训练进行，`λₖ` 平滑过渡到1，逐渐引入完整的词元级信用分配。
    6.  **策略优化**：最终的梯度更新目标函数与GRPO基本相同，唯一区别是将原本统一的优势 `Ai` 替换为经过词元权重加权后的 `Âᵢ,ₜ = sg(wᵢ,ₜ) * Ai`。其中 `sg()` 表示停止梯度传播，确保权重只用于重新分配优势，其本身不参与学习。

#### 3. 实验设计

*   **数据集**：训练使用 **HIR-16k** 数据集。该数据集中的每个样本同时包含原始提示和无指令列表的提示，天然适配CoRT对 `x+` 和 `x-` 的需求。
*   **评估基准（Benchmark）**：使用了四个指令遵循基准：
    *   **IFBench** 和 **IFEval**：测量严格提示级和指令级准确率。
    *   **MultiDimIF**：测量多维度指令约束的准确率。
    *   **AdvancedIF**：使用 o3-mini 作为裁判进行基于准则的评估。
*   **对比方法**：
    *   **基线方法**：Instruct（预训练后指令微调）、SFT（监督微调）、DPO（直接偏好优化）、GRPO（响应级强化学习基线）。
    *   **对比方法**：**RTT (Rubrics-to-Tokens)**，这是最直接相关的基线方法，它通过学习一个词元级相关性判别器（token-level relevance discriminator）来解决同一个问题。
    *   **提出的方法**：**CoRT**。
*   **奖励粒度**：在训练中使用了两种奖励模式：
    *   **CSR (Constraint Satisfaction Rate)**：满足的准则占比。
    *   **AON (All-or-Nothing)**：只有所有准则都满足才获得奖励。

#### 4. 资源与算力

*   **论文未明确说明具体的GPU型号、数量和训练时长。** 仅提及了训练相关超参数，例如：prompt batch size = 64, sample 8 responses per prompt, rollout temperature 1.0, actor learning rate = 1e-6等，但未给出完整的计算资源描述。

#### 5. 实验数量与充分性

*   **实验数量丰富，覆盖多个维度**：
    *   **主要对比**：在Qwen3-4B和Qwen2.5-7B两个模型上，分别采用CSR和AON奖励进行训练，并在4个基准上进行评估，构成了一个5（方法）x 2（模型）x 2（奖励模式）的实验矩阵，结果全面。
    *   **模型规模扩展**：在更大的Qwen3-14B模型上验证了CoRT的有效性（对应Table 2）。
    *   **策略优化算法兼容性**：在DAPO和GSPO两种不同的策略优化目标上集成CoRT，证明了其与多种优化框架的兼容性（对应Table 3）。
    *   **消融实验**：对CoRT内部的核心组件（响应归一化、SmoothStep调度）进行了消融研究，通过训练诊断图和后续验证指标分析了各组件的作用（对应Figure 3, 4, 10）。
    *   **鲁棒性分析**：对通用能力（Math500, GPQA-Diamond, MMLU-Pro）进行了检查，以确保指令遵循优化没有显著损害模型的其他能力（对应Table 5）。
    *   **案例研究**：深入分析了反事实重放在实际案例中的效果，验证了高对比度词元与准则要求的对应关系（对应Tables 6-9）。
*   **实验充分性评估**：客观地说，实验设计是**相当充分且公平的**。
    *   实验覆盖了不同的模型尺寸、不同的奖励粒度和不同的策略优化算法，体现了方法的通用性。
    *   对比方法（特别是RTT）是在相同评估协议下进行的，确保了比较的客观性。
    *   消融实验系统地揭示了各组件的作用，增强了方法的可信度。
    *   通用性能力检查表明CoRT方法并未以牺牲模型基础能力为代价。

#### 6. 主要结论与发现

1.  **有效性显著**：CoRT在绝大多数比较中（不同模型、不同奖励设置、不同基准）都优于传统的响应级GRPO，平均提升高达4.4个百分点。
2.  **与学习方法竞争力强**：CoRT无需像RTT一样训练一个独立的词元级判别器，但在性能上与之竞争甚至常常更优，证明了策略内部信号的有效性。
3.  **稳定训练的必要性**：响应归一化和SmoothStep调度对于维持训练稳定至关重要，能有效防止权重偏移和梯度/熵值激增。
4.  **兼容性好**：CoRT能够轻松集成到DAPO和GSPO等不同的策略优化目标中，并带来一致的性能提升，表明其应用范围不限于标准GRPO。

#### 7. 优点

*   **方法创新性强**：首次提出利用“反事实重放”对比从策略自身提取词元级信用信号，思路新颖，巧妙地绕过了训练额外模型的需求。
*   **计算开销小（Lightweight）**：相比RTT等需要单独训练一个“相关性判别器”的方法，CoRT仅仅增加了一次对已有响应进行评分的前向传播，无需额外生成、调用验证器或收集人工标注数据，效率极高。
*   **保持框架简洁**：CoRT只改变了优势信号在响应内部的分配方式，不改变响应级奖励、优势计算、组归一化等GRPO核心结构，易于实现和集成。
*   **实验设计全面且深入**：不仅展示了在多个标准设置下的性能提升，还通过详尽的消融、兼容性、鲁棒性和案例研究，有力地支撑了其核心论点。

#### 8. 不足与局限

*   **信号依赖性强**：CoRT的信号依赖于“准则对词元似然的影响”。当评分标准对生成词元的似然影响较弱（如内容层面的“相关性”准则）、或者准则与原始提示冗余度过高时，其代理信号的效力可能会减弱。
*   **未提及算力消耗**：论文没有明确说明实验所需的GPU、时长等具体算力成本，对于想要复现或评估其计算资源的用户来说这是一个信息缺口。
*   **实验领域集中**：所有实验和基准都聚焦于“指令遵循”这一任务，CoRT是否在其他需要细粒度信用分配的领域（如长程推理、Agent任务、代码生成）同样有效，尚需进一步探索。
*   **未讨论超参数敏感性**：论文没有分析CoRT中引入的新超参数（如：`b`, `τ`, `η`, 调度步长 `K`）对结果的敏感性。一个较强的反事实对比也可能来自错误的超参数选择导致的不稳定。

（完）
