---
title: "A Control System, a Dataset, and a Recipe for Making Frozen LLM Agents Learn a Domain"
title_zh: 一个控制系统、一个数据集以及一种使冻结的LLM智能体学习领域知识的方法
authors: Debjyoti Paul
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25415v1"
tags: ["query:rl"]
score: 9.0
evidence: 经典强化学习策略学习冻结LLM智能体控制套件
tldr: 生产LLM代理常由冻结模型配合外部harness（提示、工具、记忆等）构成，现有harness优化方法成本高或不可审计。本文提出将harness视为固定小动作空间，用ε-greedy上下文bandit和REINFORCE在线学习策略，基于多目标奖励（任务成功、验证分、合规、成本等）进行优化。在工具使用、HumanEval和HotpotQA三个任务及本地Ollama、AWS Bedrock两个模型上验证，无需昂贵搜索或自修改代码。公开了控制系统代码、数据集和部署配方，支持可审计的冻结LLM代理领域适应。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25415v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 998, \"height\": 819, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25415v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1537, \"height\": 373, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25415v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1561, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25415v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1177, \"height\": 266, \"label\": \"Table\"}]"
motivation: 现有harness优化方法不可审计或依赖黑盒API，需更受限、可解释的在线学习方案。
method: 将harness设为固定动作空间，用ε-greedy上下文bandit和REINFORCE在线学习策略，基于多目标奖励评分。
result: 在工具使用、HumanEval、HotpotQA任务及Ollama、Bedrock模型上验证了方法的有效性和可审计性。
conclusion: 该控制系统、数据集和配方为冻结LLM代理的领域适应提供了可审计、高效且与模型无关的解决方案。
---

## 摘要
生产级的LLM智能体越来越多地由冻结模型和外部框架组装而成：框架包括提示模板、工具集、记忆/检索层、规划策略和验证策略。两个2026年的系统，Meta-Harness (Lee et al., 2026) 和 HyperAgents (Meta AI, 2026) 表明，这一框架本身可以通过智能体提议器进行优化甚至自我重写——但代价是需要昂贵的代码搜索循环或不受约束的自修改代码，这两种方式都无法审计，也无法在完全黑盒模型API中使用。我们采取了一种更狭窄、更受约束的立场：将框架视为一个小的、固定的人类可读动作空间，并利用经典的样本高效强化学习（ε-贪婪上下文赌博机和REINFORCE）在线学习该空间上的策略，根据多目标奖励（任务成功率、验证器分数、策略合规性、成本、延迟以及无依据声明惩罚）进行评分。我们使用DSPy (Khattab et al., 2024) 作为上下文组装器和最强非自适应基线（DSPy BootstrapFewShot静态提示）的来源，实例化了这一控制系统，并在三个可验证任务领域（工具使用工作流、代码生成（HumanEval）和多跳检索问答（HotpotQA））以及两个模型提供商（本地Ollama模型和AWS Bedrock）上进行了评估。我们发布了框架控制系统代码、跨领域可验证任务套件、训练过程中的完整轨迹/奖励分解日志，以及一种供应商无关的部署方法，用于将其应用到新组织的领域和验证设置中。

## Abstract
Production LLM agents are increasingly assembled from a frozen model wrapped in a harness: a prompt template, a tool set, a memory/retrieval layer, a planning strategy, and a verification policy. Two 2026 systems, Meta-Harness (Lee et al., 2026) and HyperAgents (Meta AI, 2026), show that this harness can itself be optimized or even self-rewritten by an agentic proposer -- at the cost of either an expensive code-search loop or unconstrained self-modifying code, neither of which is auditable or usable with a fully black-box model API. We take a narrower, more constrained position: treat the harness as a small, fixed, human-legible action space and learn a policy over it online with classic sample-efficient reinforcement learning (an $ε$-greedy contextual bandit and REINFORCE), scored against a multi-objective reward (task success, verifier score, policy compliance, cost, latency, and an unsupported-claim penalty). We instantiate this control system with DSPy (Khattab et al., 2024) as both the context assembler and the source of the strongest non-adaptive baseline (a DSPy BootstrapFewShot static prompt), and evaluate it across three verifiable task domains -- tool-use workflows, code generation (HumanEval), and multi-hop retrieval QA (HotpotQA) -- and two model providers (a local Ollama model and AWS Bedrock). We release the harness-control-system code, the cross-domain verifiable task suite, the full trajectory/reward-decomposition logs from training, and a provider-agnostic deployment recipe for applying this to a new organization's domain and verification setup.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：生产级 LLM 智能体通常由冻结模型 + 外部框架（harness）组成，框架包括提示模板、工具集、记忆/检索层、规划策略、验证策略等。已有工作（Meta‑Harness, HyperAgents）允许智能体自动优化甚至重写框架代码，但存在三大缺陷：
  - **成本高昂**：需要额外的代码搜索循环或自修改代码，消耗大量模型调用。
  - **不可审计**：回归是 diff 而非对明确杠杆的更改，难以溯源。
  - **依赖黑盒 API**：需要代码执行能力，不适用于仅提供纯聊天补全接口的生产环境。
- **动机**：对于大量实际部署场景，需要一个更受限、可审计、低成本且与模型无关的框架优化方案。
- **整体含义**：本文提出将框架视为一个固定的小动作空间（6 个杠杆，共 729 种配置），使用经典强化学习（ε‑greedy 上下文 bandit 和 REINFORCE）在线学习最优策略，从而在保持可审计性的同时实现领域适应。

## 2. 方法论

### 核心思想
- 将框架（harness）分解为 6 个离散杠杆，每个杠杆有若干选项，形成枚举动作空间（共 3⁵×3=729 种配置）。
- 使用 DSPy 作为上下文组装器，根据选择的配置动态构造提示和模块。
- 使用多目标奖励函数，包括任务成功率、验证器分数、合规性、无依据声明惩罚、令牌成本和延迟成本。
- 在线学习策略：分别用 ε‑greedy 上下文 bandit（线性价值估计）和 REINFORCE（softmax 策略，线性特征 logits，熵正则化）在 729 个动作上学习，与随机搜索和 DSPy 优化的静态基线对比。

### 关键技术细节
- **杠杆定义**：
  - prompt style：direct / structured / reflective
  - tool policy：never / when needed / always（QA 领域对应检索强度）
  - memory policy：none / similar successes / successes and failures
  - planning policy：none / brief plan / plan and revise
  - verification policy：none / final check / stepwise
  - max steps：2 / 4 / 6
- **奖励函数**：R = wₛ·success + wᵥ·verifier score + w_c·compliance − w_u·unsupported claims − w_$·tokens/1000 − w_ℓ·latency/1000。成功率由各领域确定性验证器给出（单元测试、状态 diff、精确匹配等）。
- **控制器**：
  - **随机**：均匀采样 729 种配置，作为下界。
  - **DSPy‑static**：使用 DSPy 的 BootstrapFewShot 在少量标签数据上优化一次，得到固定配置（structured prompt, when‑needed tools/retrieval, brief plan, final‑check verification），作为最强的非自适应基线。
  - **Bandit / REINFORCE**：在线学习，主实验每个 run 25–60 episodes。

## 3. 实验设计

### 数据集 / 场景
- 三个可验证任务领域，共 120 个任务（96 训练 / 24 测试）：
  - **工具使用（Tool‑use）**：40 个合成多工具 CRM 工作流（创建/更新/删除/合并操作），通过状态 diff 评分。
  - **代码生成（Coding）**：40 个 HumanEval 问题，通过执行官方单元测试评分。
  - **多跳检索问答（Retrieval QA）**：40 个 HotpotQA 问题（使用干扰项设置），通过精确匹配评分，token F1 作为软验证信号。

### 基准（Benchmark）
- 对比方法：随机搜索（Random）、DSPy‑static 静态基线、ε‑greedy bandit、REINFORCE。

### 模型提供方
- **Ollama**（本地，qwen2.5:7b）
- **AWS Bedrock Haiku**
- **AWS Bedrock Sonnet**（作为更强模型的补充实验，仅 2 seeds × 40 episodes）

### 实验配置
- 主矩阵：4 优化器 × 3 领域 × 2 模型（Ollama, Bedrock Haiku），Ollama 每 seed 25 episodes，Bedrock Haiku 每 seed 60 episodes（3 seeds）。
- 附加实验：在工具使用 / Bedrock Haiku 上运行 300 episodes（2 seeds）测试样本效率。
- 主矩阵共 2,460 episodes，加上额外 1,200 episodes 和 Sonnet 实验 960 episodes，总计约 4,620 logged episodes。

## 4. 资源与算力

- 论文未明确说明使用的 GPU 型号、数量或训练时长。
- 提及总花费 $7.62，使用的是本地 Ollama（串行，约 2.7 episodes/min）和 AWS Bedrock（可扩展并发）。
- DSPy 的 LM 磁盘缓存曾被误认为零成本，但已修复并重新运行全矩阵。
- 作者未详细列出硬件资源，仅说明算力成本极低。

## 5. 实验数量与充分性

- **实验数量**：主矩阵 2,460 episodes，附加样本效率实验 1,200 episodes，Sonnet 实验 960 episodes，总计约 4,620 条带完整轨迹日志的记录。每个条件下 1–3 个随机种子。
- **充分性分析**：
  - 作者坦诚承认主要限制：每个 online 控制器仅 25–60 episodes，远小于动作空间 729，导致统计力不足。
  - 额外 300 episodes 实验显示 bandit 和 REINFORCE 仍未收敛到静态基线水平，说明样本效率确实是瓶颈。
  - 实验设计较为公平：使用相同任务、相同验证器、相同 DSPy 组装器；修复了冷启动和崩溃隔离问题；对比了随机、静态、在线方法。
  - 但缺乏 OpenAI API 的支撑，仅覆盖两个模型提供商；工具使用领域为合成环境；无依据声明度量仅为检索领域的词汇重叠启发式。
  - 总体而言，实验在当前预算下是透明的，但作者明确指出需要更大 episode 预算才能得出更可靠结论。

## 6. 主要结论与发现

- **核心发现**：**DSPy‑优化的静态基线在几乎所有领域和模型上匹配或超过了在线自适应控制器（bandit 和 REINFORCE）**，并且通常使用更少的令牌成本（例如工具使用 / Bedrock Haiku：DSPy‑static 96% 成功率，285 tokens/episode；REINFORCE 62%，680 tokens）。
- **原因**：这不是在线控制器的天花板，而是**样本效率缺口**——当动作空间 M=729 时，需要 Ω(M) 量级的样本才能有效学习，而主实验仅给 25–60 episodes。即便是 300 episodes 的扩展实验，在线控制器仍远低于 DSPy‑static 的水平。
- **实用建议**：在实际部署中应该先用 DSPy 优化出静态基线，再在其基础上部署在线控制器（即预热启动），而不是从均匀先验开始在线学习。
- **其他发现**：
  - 冷启动陷阱：bandit 的 argmax 默认索引 0 导致全零初始化后陷入无效配置，通过乐观初始化和随机打断解决。
  - 崩溃隔离：强模型使用 plan‑and‑revise 时可能因响应格式错误拖垮整个任务，通过 try/except 将解析失败视为普通失败 episode 解决。
  - DSPy 缓存导致令牌计数错误，通过字符长度回退估计修正。

## 7. 优点

- **可审计性**：每个决策都是少数预先审核的配置之一，回归可归因于具体杠杆，而非代码 diff。
- **模型无关**：无需代码执行能力，适用于纯聊天补全 API（黑盒）。
- **多目标奖励**：合规性和无依据声明率作为首要信号，而非单一任务准确率。
- **开源贡献**：发布了控制系统代码、120 个可验证任务、4,620 条轨迹日志、部署配方（RECIPE.md）。
- **诚实报告负面结果**：直接报告静态基线优于在线控制器的反直觉结论，并深入分析原因，对实践者具有指导意义。
- **修复了常见实践陷阱**：冷启动、崩溃隔离、缓存计数错误，并公开这些经验。

## 8. 不足与局限

- **样本效率不足**：主实验每 run 仅 25–60 episodes，对于 729 动作空间严重不足，导致在线控制器无法充分展示潜力，结论可能偏向静态基线。
- **模型覆盖有限**：未能使用 OpenAI API（密钥被拒），仅涵盖 Ollama 和 AWS Bedrock 两个提供方，通用性受限。
- **领域合成化**：工具使用领域是模拟 CRM 后端，非真实生产系统。
- **度量粗糙**：无依据声明惩罚仅用于检索领域的词汇重叠启发式，未实现通用幻觉检测。
- **预算控制不足**：总花费 $7.62 超出了预设的单次 $5 上限（虽然各单次 run 均未超，但整体未设置全局 cap）。
- **统计力有限**：最多 3 个种子，未进行更严格的置信区间分析（虽然提供了 bootstrap CI）。
- **实验范围可扩展**：建议的未来工作（如用静态基线初始化在线控制器权重）未在当前论文中验证。

（完）
