---
title: "Theoretical Foundations of $\\max$@$k$ Reinforcement Learning"
title_zh: "$\\max$@$k$ 强化学习的理论基础"
authors: "Riccardo Poiani, Martino Bernasconi, Andrea Celli"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17823v1"
tags: ["query:rl"]
score: 8.0
evidence: "面向LLM推理任务的max@k强化学习理论基础"
tldr: "针对大模型推理任务中常用的max@k评估指标，本文理论研究了有限时域强化学习下的max@k学习问题。发现马尔可夫策略无法达到最优，提出状态增广方法恢复最优性，并揭示了历史依赖策略的必要性。进一步证明了学习max@k最优策略的统计困难度，并设计了达到最优样本复杂度的算法。该工作为retry-aware强化学习奠定了理论基础。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17823v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 583, \"height\": 324, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17823v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 420, \"height\": 282, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17823v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 879, \"height\": 1058, \"label\": \"Figure\"}]"
motivation: "现有强化学习理论主要针对期望回报最大化，缺乏对max@k这类retry-aware指标的理论基础。"
method: 通过状态增广将问题转化为标准RL，并分析历史依赖策略与马尔可夫策略的性能差距。
result: 证明马尔可夫策略不足，给出最优策略复杂度下界和最优算法。
conclusion: "为max@k强化学习提供系统理论，指导实际算法设计。"
---

## 摘要
强化学习是现代大型推理模型的核心技术。通常，对于代码生成和定理证明等困难任务，智能体通过生成 $K$ 个回答而非单个样本来评估，并使用诸如 $\max$@$k$ 这类具有重试意识的指标来衡量性能。尽管这些标准在实际中很重要，但其学习过程的理论基础仍然有限。本文对有限时域强化学习中的 $\max$@$k$ 学习问题进行了理论研究。我们证明，优化 $\max$@$k$ 目标与标准期望回报最大化有本质区别。具体而言，我们证明了马尔可夫策略通常不充分，识别了一种能恢复最优性的紧凑状态增广，并明确刻画了历史依赖策略与非历史依赖策略之间可能出现的性能差距。此外，我们表明学习 $\max$@$k$ 最优策略在统计上比标准强化学习更困难，并提供了一种达到最优样本复杂度的高效算法。

## Abstract
Reinforcement Learning is a cornerstone technique for modern large reasoning models. Usually, for difficult tasks such as code generation and theorem proving, the agent is evaluated by generating $K$ responses rather than sampling a single response, and performance is then measured using a retry-aware metric such as $\max$@$k$. Despite their practical importance, the theoretical foundations of learning under such criteria remain limited. In this work, we provide a theoretical study of the $\max$@$k$ learning problem in finite-horizon reinforcement learning. We show that optimizing the $\max$@$k$ objectives is fundamentally different from standard expected-return maximization. In particular, we prove that Markovian policies are in general insufficient, identify a compact state augmentation that restores optimality, and explicitly characterize the performance gap that can arise between history-dependent and non-history-dependent policies. Moreover, we show that learning $\max$@$k$-optimal policies is statistically harder than standard reinforcement learning and provide an efficient algorithm that achieves the optimal sample complexity rate.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：在大语言模型（LLM）推理任务（如代码生成、定理证明）中，评估指标常采用 $\max@k$，即允许模型生成 $K$ 个回答，取其中最高奖励。然而，标准强化学习理论主要关注期望累积回报最大化，而 $\max@k$ 是回报分布的非线性函数，现有理论无法直接适用。实践中已有许多优化 $\max@k$ 的启发式方法（如推理感知梯度估计），但缺乏严谨的理论基础。
- **整体含义**：本文首次系统地建立了 $\max@k$ 强化学习的理论基础，揭示了其与标准强化学习的本质区别，包括策略空间需求、统计复杂度变化和计算复杂性，为设计和评估 retry-aware 的推理算法提供了理论指导。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程（文字说明）

- **核心思想**：
  1. **非马尔可夫性**：证明马尔可夫策略不足以达到 $\max@k$ 最优，必须使用历史依赖策略。并通过构造例子展示两者之间存在常数倍的性能差距。
  2. **信息压缩**：证明最优策略仅需依赖两个压缩信息：之前 $i-1$ 次 rollout 的最大回报 $M_i$ 和当前第 $i$ 次 rollout 内已累积的回报 $G_{h,i}$，而不需要完整历史。这定义了策略类 $\Pi^C$。
  3. **近似规划**：通过离散化回报空间（网格 $\kappa$），将最优策略求解转化为有限状态动态规划，给出多项式时间近似方案（PTAS），时间复杂度 $O(K S^2 A H^3/\kappa^2)$。
  4. **精确规划困难性**：证明即使对于 $K=2$，决定动作的优劣也是 NP-hard（归约于子集和问题）。
  5. **学习算法**：采用均匀采样生成模型，构造经验转移核，再对离散化 MDP 进行动态规划。通过精细的方差分析得到样本复杂度上界 $\tilde{O}(K S A H^3/\epsilon^2)$，与下界匹配。

- **关键技术细节**：
  - **最优性方程**：改进了 Bellman 方程，将当前步价值函数表示为 $V^*_{h,i}(H_{h,i}) = \max_a \sum_{s'} p_h(s'|s,a) V^*_{h+1,i}(H_{h,i}\circ(a,s'))$，边界条件考虑跨 rollout 的 max 操作。
  - **压缩表示**：$\Phi(H_{h,i}) = (s_{h,i}, M_i, G_{h,i})$，证明 $\Pi^C$ 中总存在最优策略。
  - **离散化误差分析**：通过引理 1 证明同一策略在不同奖励函数下的价值差不超过 $H\|r-\tilde{r}\|_\infty$，进而得到近似算法误差 $2H\kappa$。
  - **学习下界**：构造一个 base 实例和一系列替代实例（将某个动作的成功概率提高 $\Delta$），利用反证法和 change-of-measure 导出 $\Omega(K S A H^3/\epsilon^2 \log(1/\delta))$。
  - **算法流程**：
    1. 对每个 $(s,a,h)$ 采样 $m$ 次，构造 $\hat{p}_h$。
    2. 离散化奖励为 $\kappa = \epsilon/(9H)$。
    3. 对增广状态空间 $X = S \times \lceil H/\kappa\rceil \times \lceil H/\kappa\rceil$ 进行动态规划，得到 $\hat{\mu}$。
    4. 输出 $\hat{\mu}$。

## 3. 实验设计

- **本文为纯理论论文，未进行任何实验或数据集验证。**
- 所有主张均通过数学证明支持，包括构造性例子、归约证明、信息论下界等。
- 因此，无实验场景、基准方法和对比方法。

## 4. 资源与算力

- **未提及任何计算资源（GPU、训练时长等）**。论文不涉及实际运行，所有分析均为理论推导。

## 5. 实验数量与充分性

- **无实验**。理论结果通过严谨的数学推导证明，符合理论机器学习的标准范式。充分性体现在：
  - 下界和上界匹配，证明样本复杂度最优。
  - 规划方面给出 PTAS 和 NP-hard 结果，证明近似算法是最佳可能。
  - 通过具体反例说明马尔可夫策略和压缩表示的必要性。
- 由于是理论工作，不存在实验偏差或公平性问题。

## 6. 论文的主要结论与发现

1. **马尔可夫策略非最优**：存在实例使得最优历史依赖策略比任何马尔可夫策略好至少 1.24 倍常数因子。
2. **状态压缩定理**：最优策略只需知道当前状态、之前最佳回报和当前累积回报，无需完整历史。
3. **多项式时间近似规划**：存在 PTAS 以 $O(K S^2 A H^5/\epsilon^2)$ 时间计算 $\epsilon$-最优策略。
4. **精确规划 NP-hard**：即使 $K=2$ 且固定策略集合极小，判断动作优劣也是 NP-hard。
5. **统计困难度翻倍**：学习 $\max@k$ 最优策略的样本复杂度下界为 $\Omega(K S A H^3/\epsilon^2 \log(1/\delta))$，与标准 RL 相比多出因子 $K$。
6. **最优算法**：设计了均匀采样 + 动态规划算法，达到匹配的上界 $\tilde{O}(K S A H^3/\epsilon^2)$（时间齐次），证明下界紧。

## 7. 优点

- **理论完整性**：同时覆盖规划和学习两个子问题，并给出下界和上界，形成闭环。
- **揭示核心差异**：清晰指出 $\max@k$ 与标准 RL 在策略空间和样本复杂度上的本质不同。
- **实用指导意义**：压缩表示结果与实践中自适应推理方法（如 self-refine、搜索推理）的设计思想高度吻合，提供了理论解释。
- **证明手段丰富**：包括构造性反例、归约、信息论下界等，论证扎实。
- **计算复杂性刻画**：给出 PTAS 和 NP-hard 结果，精确说明问题的可解性边界。

## 8. 不足与局限

- **缺乏实证验证**：作为理论论文未进行实验，因此无法评估理论结果在实际 LLM 推理任务中的适用性和常数因子。
- **模型假设限制**：假设有限状态、有限动作，且奖励有界 $[0,1]$，不完全适配大语言模型连续策略空间的实际情况。
- **生成模型假设**：学习部分假设存在生成模型（可任意采样任意状态-动作对的下一状态），而在标准在线交互场景中样本复杂度可能更高，需进一步研究。
- **离散化精度依赖**：近似规划算法依赖 $\kappa$，实际选择 $\kappa = \epsilon/H$ 可能导致状态空间随 $H$ 和 $1/\epsilon$ 增大，虽多项式但实际计算可能昂贵。
- **未考虑函数近似**：所有结论基于表格情形，扩展到深度强化学习仍需后续工作。
- **只针对有限时域**：未扩展到折扣无限时域设置。

（完）
