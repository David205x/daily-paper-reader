---
title: "Where Should RL Post-Training Compute Go? Model Size, Search, Learning, and Feedback"
title_zh: 强化学习后训练的计算资源应投向何处？模型规模、搜索、学习与反馈
authors: "Patrick Wilhelm, Odej Kao"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13389v1"
tags: ["query:rl"]
score: 8.0
evidence: RL后训练计算分配用于LLM
tldr: 强化学习后训练面临固定计算预算下的资源分配问题。本文提出FLOP计数框架GRPO，分解为搜索、学习、反馈等开销。实验发现，最佳分配随模型规模、预算和奖励系统变化，且模型选择与训练策略耦合。RACE协议作为诊断工具，可识别不同分配 regime，强调报告时应划分计算用途。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13389v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1406, \"height\": 1012}, {\"url\": \"assets/figures/arxiv/2607.13389v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1409, \"height\": 725}, {\"url\": \"assets/figures/arxiv/2607.13389v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1409, \"height\": 979}, {\"url\": \"assets/figures/arxiv/2607.13389v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1420, \"height\": 689}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13389v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1238, \"height\": 280}]"
motivation: 固定总FLOP预算下，如何分配模型大小、搜索、学习和反馈计算尚不清晰。
method: 提出GRPO框架分解计算为rollout/搜索、policy更新/学习、反馈模型评估，并在LoRA微调Qwen2.5上实验。
result: 分配最优性依赖模型规模、预算和奖励类型；规则奖励主要算力用于rollout，PRM需额外推理。
conclusion: RL后训练论文应报告总FLOP及其在模型大小、搜索、学习和反馈间的划分。
---

## 摘要
强化学习（RL）后训练越来越多地被用于调整基础模型，以进行推理、规划和反馈驱动的机器人学习流程，但受限的后训练资源通常由单个总FLOP预算概括。我们研究了这种实践背后的固定预算决策问题：在相同的后训练预算下，应该使用更大的策略、更长时间地训练较小的策略、生成更多的展开（rollout）搜索，还是将计算用于更强的奖励反馈？我们为GRPO后训练引入了一个FLOP核算框架，将计算分解为展开/搜索、策略更新/学习以及奖励或反馈模型评估。在基于LoRA微调的Qwen2.5策略上，我们发现了条件分配边界：最佳观察到的分配随模型规模、计算预算、奖励系统和评估目标而变化。相同FLOP的模型规模比较表明，模型选择和训练分配是耦合的，因为更大的策略消耗更多的每token计算，因此在相同预算下购买更少的更新或展开。奖励系统也改变了核算：基于规则的奖励几乎将所有非更新计算用于策略展开，而PRM风格的反馈则将对奖励模型推理的预算分配置于可见部分。我们提出RACE作为一种诊断性试点网格协议，而非保证的保持改进，用于在昂贵的验证运行之前识别分配机制；我们的结果表明，RL后训练论文应报告总FLOPs，以及计算如何分配给模型规模、搜索、学习和反馈。

## Abstract
Reinforcement Learning (RL) post-training is increasingly used to adapt foundation models for reasoning, planning, and feedback-driven robot-learning pipelines, but constrained post-training resources are often summarized by a single total FLOP budget. We study the fixed-budget decision problem behind this practice: under the same post-training budget, should one use a larger policy, train a smaller policy longer, generate more rollout search, or spend compute on stronger reward feedback? We introduce a FLOP-accounting framework for GRPO post-training that decomposes compute into rollout/search, policy-update/learning, and reward- or feedback-model evaluation. Across LoRA-adapted Qwen2.5 policies, we find conditional allocation frontiers: the best observed allocation changes with model size, compute budget, reward system, and evaluation target. Same-FLOP model-size comparisons show that model choice and training allocation are coupled because larger policies consume more per-token compute and therefore buy fewer updates or rollouts under the same budget. Reward systems also change the accounting: rule-based rewards spend nearly all non-update compute on policy rollouts, while PRM-style feedback allocates a visible part of the budget to reward-model inference. We present RACE as a diagnostic pilot-grid protocol, not a guarantee of held-out improvement, for identifying allocation regimes before expensive validation runs; our results suggest that RL post-training papers should report total FLOPs together with how compute is divided among model size, search, learning, and feedback.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义

- **研究动机**：强化学习（RL）后训练是调整基础模型（尤其是用于机器人学习的推理、规划和反馈模块）的关键步骤。然而，现有工作常将后训练成本简化为一个总FLOP预算，忽略了计算在模型规模、搜索（rollout）、学习（policy update）、反馈（reward evaluation）等不同环节间的具体分配。
- **核心问题**：在固定的后训练计算预算下，应该如何分配资源？具体而言：应该使用更大的策略模型，还是更长时间地训练较小的模型？应该增加rollout搜索，还是投入更多计算用于更昂贵的奖励反馈？
- **整体含义**：RL后训练是一个受约束的资源分配问题，其最优分配策略依赖于模型规模、计算预算、奖励系统设计以及下游评估目标。论文呼吁在报告后训练结果时，不仅要给出总FLOP，还要明确分解到搜索、更新、反馈等环节。

## 2. 方法论：核心思想与技术细节

- **核心思想**：提出一个FLOP核算框架，将RL后训练总计算量分解为三个可观测的组成部分：
  - `C_search`：自回归rollout生成的计算
  - `C_learning`：策略优化（梯度更新）的计算
  - `C_reward`：奖励或验证器推理的计算
  - 总计算量：`C_total = C_search + C_learning + C_reward`
- **关键量化指标**：定义**更新分数 ρ** = `C_learning / C_total`，用于表征计算分配偏向。小ρ表示搜索或奖励密集，大ρ表示更新密集。
- **RACE诊断协议**（Reward-Aware Compute Allocation）：
  - 在有限预算下，通过一个小型IsoFLOP网格（pilot grid）测量不同ρ下的奖励响应。
  - 拟合局部响应模型（默认使用log ρ的二次函数），根据拟合稳定性推荐后续验证运行的分配方向（如内部最优、边界外推或保守选择最佳观测点）。
  - 目的：识别分配regime（高更新、低更新、奖励计算密集型），而非保证提升。
- **LoRA适配**：对于LoRA微调，更新计算使用有效更新分数 `f_eff = a + (1-a)*f_LoRA`，其中a=0.85，f_LoRA为可训练参数比例，以反映冻结骨干网络的成本。

## 3. 实验设计

- **任务与数据**：使用数学推理任务 **Polaris-53K** 作为基础训练数据集。下游评估使用 **GSM8K** 和 **MATH-500**（各250个样本）。
- **模型与训练**：
  - 策略模型：Qwen2.5 instruction-tuned系列（1.5B、3B、7B参数），采用LoRA微调（rank范围8~128）。
  - 算法：GRPO，固定K=2个采样补全，最大补全长L=2048。
- **对比的奖励系统**：覆盖从廉价到昂贵的频谱：
  - **稀疏奖励**（sparse）：仅最终答案正确性（二元）
  - **结构化奖励**（structured）：结合格式、答案形状约束
  - **密集奖励**（dense）：基于数值接近度给予部分分数
  - **代理PRM奖励**（proxy PRM）：结合过程监督信号与结果正确性
  - **真实PRM奖励**（real PRM）：使用Qwen2.5-Math-PRM-7B专用过程奖励模型
- **实验阶段**：
  - **Stage A**：模型规模与搜索-学习分配（1.5B/3B/7B，三个计算预算带，多种LoRA rank）
  - **Stage B**：固定3B策略，比较四种奖励系统（稀疏、结构化、密集、代理PRM）下的分配前沿
  - **Stage D**：固定3B策略，使用真实PRM奖励模型
- **评估指标**：原生训练奖励、归一化奖励、下游pass@1符号准确率、公共评判分数（common judge）。

## 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量及具体训练时长。仅给出了计算预算带的FLOP量级（如`C1p1e16`约为10^16 FLOPs），以及不同rank对应的更新步数（如1.5B模型在预设计算带内的步数范围46~140步）。
- 算力估计基于参数-令牌FLOP核算，但未报告实际硬件配置和执行时间。

## 5. 实验数量与充分性

- **实验组数**：
  - Stage A：3种模型规模 × 3个计算带 × 至少9个LoRA rank（含中间rank）≈ 至少81个训练运行。
  - Stage B：3个计算带 × 9个rank × 4种奖励 = 108个运行。
  - Stage D：3个计算带 × 9个rank = 27个运行。
  - 总计约216个独立训练运行（不含RACE验证的额外运行）。
- **充分性评价**：
  - **优点**：实验覆盖了模型规模、计算预算、奖励类型、评估目标等多个维度，IsoFLOP设计系统地控制了总计算量。
  - **不足**：仅使用一个数学推理数据集和一种策略模型家族（Qwen2.5），未扩展到其他领域（如机器人控制、多模态）。LoRA微调可能无法代表全参数微调的行为。种子数量有限。实验并未严格进行多次独立重复以估计方差（论文未报告标准误差）。

## 6. 主要结论与发现

1. **存在结构化分配行为**：在固定总FLOP下，不同运行在搜索-学习平面上分布不同，奖励随分配位置变化，存在可测量的响应面。
2. **分配前沿条件依赖于模型规模和奖励系统**：
   - 相同FLOP下，更大的策略模型消耗更多每token计算，导致更少的rollout和更新步数，因此模型选择与训练分配耦合。
   - 基于规则的奖励（稀疏、结构化、密集）通常偏好高更新分数（ρ≈0.72），而PRM风格的奖励（代理/真实）偏好较低更新分数（ρ≈0.44-0.45），因为PRM推理消耗了部分预算。
3. **下游评估目标可能选择不同分配**：
   - 原生训练奖励和下游pass@1符号准确率的最佳ρ可能不同。例如，pass@1上真实PRM的a180（高更新）设置表现最好，而公共评判分数（common judge）偏好a100（中等更新）。
4. **RACE诊断的有效性**：可以在小网格内正确识别高/低更新regime，但并不能保证在保持集上提升原生奖励。

## 7. 优点

- **问题视角新颖**：将RL后训练视为资源分配问题，而非简单缩放总计算量，有助于指导实际工程决策。
- **FLOP核算框架清晰**：分解搜索、更新、奖励三个计算项，暴露了不同奖励方案的本质成本差异，使比较更公平。
- **实验设计系统**：采用IsoFLOP设计、跨模型规模、多奖励类型、多评估目标，提供了丰富的条件性证据。
- **诊断协议RACE**：提供了一种低成本识别分配regime的方法，帮助减少昂贵验证运行的数量。
- **对实用场景有直接提示**：在机器人学习中，基础模型作为规划/反馈组件时，其资源分配决策可通过此框架分析。

## 8. 不足与局限

- **局限性明确列出**：论文自身指出了使用数学推理作为代理任务、单一模型家族、GRPO、LoRA、有限种子、FLOP估计仅用于相对比较、RACE不保证提升、缺乏对奖励模型容量缩放的研究等。
- **实验覆盖不足**：未涉及直接机器人控制任务、未使用全参数微调、未在多个领域（如代码、科学推理）验证。策略模型仅限Qwen2.5系，泛化性不确定。
- **统计严谨性弱**：未提供多次重复运行的波动范围（标准差），结论可能受单次运行噪声影响。RACE的二次拟合在数据上并非统计最优（常数基线更优），其作为诊断工具的可靠性依赖具体设置。
- **奖励模型成本的计算**：PRM推理成本按前向传递计费，但未考虑奖励模型本身也需要训练，这会影响总预算的实际分配。
- **下游评估规模小**：GSM8K和MATH-500各仅250个样本，可能导致评估结果方差较大。

（完）
