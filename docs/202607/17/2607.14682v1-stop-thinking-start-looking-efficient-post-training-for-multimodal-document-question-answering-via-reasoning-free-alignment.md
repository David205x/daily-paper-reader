---
title: "Stop Thinking, Start Looking: Efficient Post-Training for Multimodal Document Question Answering via Reasoning-Free Alignment"
title_zh: 停止思考，开始观察：基于无推理对齐的多模态文档问答高效后训练
authors: "Harikrishnan P M, Goutham Vignesh, Ganesh Parab, Saisubramaniam Gopalakrishnan, Vishal Vaddina, Varun V, Rohit Agrawal"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14682v1"
tags: ["query:rl"]
score: 9.0
evidence: 将分组相对策略优化(GRPO)应用于多模态文档问答，绕过中间推理令牌直接对齐视觉特征
tldr: "多模态文档问答需精确视觉定位，现有方法依赖大量标注或长推理链。本文提出Perception-RFT，用GRPO跳过中间推理，直接对齐视觉与定位输出。实验表明，推理启用的模型在4B参数下训练后抑制推理，token减少60%却不如纯感知训练。发现定位分歧（Grounding Divergence）：联合优化时语义鲁棒性与几何精度存在权衡。早期SFT→RL转换可减少65%训练数据达到相似精度，支持感知优先策略。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14682v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1733, \"height\": 985, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14682v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1760, \"height\": 515, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14682v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1760, \"height\": 514, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14682v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 849, \"height\": 397, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14682v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 836, \"height\": 656, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14682v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1479, \"height\": 1876, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14682v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1763, \"height\": 584, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14682v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 862, \"height\": 300, \"label\": \"Table\"}]"
motivation: 当前多模态文档QA的精确视觉定位面临SFT数据需求大和优化停滞，以及推理RL导致的高token成本问题。
method: 提出Perception-RFT，基于GRPO直接对齐视觉特征与结构化定位输出，无需中间推理token。
result: "推理启用模型训练中抑制推理，token减少60%以上但性能低于纯感知训练；发现定位分歧，语义与几何精度存在权衡。"
conclusion: "早期SFT→RL转换减少65%训练数据可达相似精度，验证感知优先训练有效性。"
---

## 摘要
高效的多模态文档问答结合显式视觉定位——即定位支撑每个答案的精确文档区域——仍然是一个开放挑战。当前方法分为监督微调（SFT）和以推理为中心的强化学习（RL）。前者需要大量标注数据集且会达到优化平台期，后者依赖于冗长的中间推理链，这会增加推理标记成本且无明显收益。我们提出 Perception-RFT，一种将组相对策略优化（GRPO）应用于多模态文档问答的训练框架，跳过中间推理标记，直接对齐视觉特征与结构化定位输出。为严格评估推理的必要性，我们在相同奖励设置下构建了一个推理变体。我们发现，启用推理的模型在训练过程中会抑制其推理链，在4B参数规模下收敛到直接的基于感知的策略，将每次查询的推理标记长度减少超过60%，而启用推理的RL表现不如仅感知训练。通过对Qwen3-VL-4B优化动态的细粒度分析，我们确认了文本领域后训练中存在的SFT饱和与冷启动RL不稳定现象扩展到了多模态领域，并识别出一种先前未被描述的定位发散现象：在联合RL优化下，两个分布外（OOD）基准（共4,828个样本）上语义鲁棒性与几何精度之间存在选择性权衡。我们还进一步表明，早期SFT→RL转换可以在减少65%训练数据的情况下达到相当的精度。

## Abstract
Efficient multimodal document question answering with explicit visual grounding, locating the precise document region that supports each answer remains an open challenge. Current approaches bifurcate into Supervised Fine-Tuning (SFT), which requires large annotated datasets and reaches optimization plateaus, and reasoning-centric Reinforcement Learning (RL), which depends on verbose intermediate traces that inflate inference token cost without clear benefit. We introduce Perception-RFT, a training framework that applies Group Relative Policy Optimization (GRPO) to multimodal document QA, bypassing intermediate reasoning tokens to directly align visual features with structured grounding outputs. To rigorously evaluate the necessity of reasoning, we construct a reasoning variant under identical reward settings. We find that reasoning-enabled models suppress their reasoning traces during training, converging to direct perception-based policies at the 4B parameter scale, reducing per-query inference token length by more than 60%, while reasoning-enabled RL underperforms perception-only training. Through a fine-grained analysis of Qwen3-VL-4B optimization dynamics, we confirm that SFT saturation and cold-start RL instability established in text-domain post-training extend to multimodal, and identify a previously uncharacterized Grounding Divergence: a selective trade-off between semantic robustness and geometric precision on two out of distribution (OOD) benchmarks (4,828 samples) under joint RL optimization. We further show that an early SFT$\rightarrow$RL transition achieves comparable precision with 65% less training data.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：多模态文档问答（Multimodal Document QA）不仅需要生成正确的文本答案，更需要定位支撑答案的精确视觉区域（即文档视觉定位，DVG）。然而现有方法面临两难：监督微调（SFT）依赖大规模标注且优化易饱和；以推理为中心的强化学习（RL）则需要冗长的中间推理链，增加推理成本且无明确收益。
- **核心问题**：在文档视觉定位这类感知主导的任务中，显式的中间推理是否必要？能否通过跳过推理、直接对齐视觉特征与空间输出来实现更高效的训练？
- **整体含义**：论文挑战了“SFT记忆、RL泛化”的普遍观点，证明在感知主导任务中，**直接感知对齐优于推理介导生成**，并揭示了SFT饱和、冷启动RL不稳定以及“定位发散”等新现象。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：提出**Perception-RFT**框架，将组相对策略优化（GRPO）应用于文档视觉定位，直接强制模型跳过中间推理，将视觉特征映射为结构化输出（JSON格式的答案+边界框）。
- **关键技术细节**：
  - **直接感知约束**：通过系统提示强制模型“不要输出推理步骤，仅输出最终JSON结果”。
  - **GRPO优化**：对每个查询采样一组输出，基于归一化奖励分布计算优势，目标函数为：
    \[
    J(\theta) = \mathbb{E}_{q\sim D}\left[\frac{1}{G}\sum_{i=1}^G \left(\frac{\pi_\theta(o_i|q)}{\pi_{\text{old}}(o_i|q)} A_i - \beta D_{\text{KL}}(\pi_\theta\|\pi_{\text{ref}})\right)\right]
    \]
    其中 \(A_i\) 为组内归一化优势。
  - **门控密集奖励机制**：总奖励 \(R = R_{\text{format}} + R_{\text{sem}} + R_{\text{loc}}\)，由三个分量组成：
    - 格式一致性 \(R_{\text{format}}\)：二进制，仅当输出为合法JSON且包含必要键时取1。
    - 语义精度 \(R_{\text{sem}}\)：基于ANLS的二次缩放，精确匹配取1，否则为ANLS²。
    - 门控定位奖励 \(R_{\text{loc}}\)：分为**塑形状态**（IoU<0.6时线性缩放，λ=0.3）和**成功状态**（IoU≥0.6时跳跃至1.0），避免模型陷入次优重叠。
  - **对比变体**：构造**Reasoning-RFTb**，允许模型生成<think>推理链，但奖励设置与感知版本完全相同，用于隔离推理的效果。

## 3. 实验设计

- **数据集**：
  - **训练集**：从DocILE和FormNLU聚合的23,696个QA样本，涵盖多种财务文档（税务发票、采购订单、收据、水电费账单等）。
  - **ID评估集**：6,194个相同财务领域样本。
  - **OOD评估集**：两个独立基准——DOGR-Bench（800个样本，含海报、图表、学术PDF）和MMDocBench（4,028个样本，含财务报告、收据、科学论文、信息图），共4,828个OOD样本。
- **基准（Benchmark）**：ID上的精准联合成功（F1_all），OOD上分别报告三个指标：F1_EM（精确文本匹配）、F1_loc（IoU≥0.5的定位成功）、F1_all（联合成功）。
- **对比方法**：
  - 内部基线：Qwen3-VL-4B（零样本）、SFT、RFTs（SFT→RL）、RFTb（冷启动RL）、Reasoning-RFTb（推理变体）。
  - 外部参考：Gemini 3.0 Flash（零样本）、DOGR（SFT，仅在DOGR-Bench）。
  - 排除了DocThinker（因无公开可复现资源）。

## 4. 资源与算力

- **硬件**：单块 NVIDIA A100（80GB），使用 Unsloth 和 LoRA（r=16, α=16）进行内存高效微调。
- **训练步数**：SFT 3个epoch（1113步）；RFTs用6k样本（不到SFT计算量的10%），GRPO组大小G=8；RFTs-Early仅用300步SFT后接6k RL步。
- **总数据量**：SFT约71k样本-曝光，RFTs-Early约19k+6k，减少65%训练数据。
- 论文未明确说明训练总时长，但明确强调单卡可完成。

## 5. 实验数量与充分性

- **实验组数**：在ID和OOD上测试了5种内部配置+1个外部基线，并进行了消融（推理变体、早期切换）。
- **动态分析**：报告了6个检查点（图2、3、4、5），跟踪F1_EM、F1_loc、F1_all随训练步数的变化。
- **充分性**：
  - 覆盖了SFT饱和、冷启动RL、推理对比等关键维度，区分了ID和OOD场景，实验设计系统、对比公平（同一奖励函数、同一模型骨干）。
  - 但仅使用4B参数模型，未扩展至更大规模；OOD仅两个基准，且未包含更广泛的场景（如手写文档、多语言）。
  - 推理分析仅限于冷启动RL（Reasoning-RFTb），未构建推理标注的SFT→RL流水线（因缺乏此类数据）。
- **客观性**：指标严格，未使用文本归一化；三次独立运行取均值，结果可靠。

## 6. 主要结论与发现

1. **SFT饱和**：SFT在ID上达到平台期后，RL（RFTs）能继续提升联合定位精度（F1_all从0.668→0.718）。
2. **冷启动RL不稳定**：直接对基模型做RL（RFTb）远不如先SFT再RL（RFTs），证实文本领域的冷启动问题扩展到了多模态。
3. **推理冗余**：Reasoning-RFTb在4B规模下最终会压缩甚至消除推理链（平均推理token减少62%），且性能低于感知版本（RFTb F1_all 0.411 vs 0.303；OOD上0.600 vs 0.382）。推理引入不稳定性（图4）。
4. **定位发散（Grounding Divergence）**：在OOD上，定位精度（F1_loc）转移良好，但语义提取（F1_EM）可能退化（如DOGR-Bench上所有训练变体均低于零样本），且RFTb相比RFTs能更好地保持语义灵活性。
5. **早期SFT→RL有效**：RFTs-Early仅用19k SFT样本+6k RL，在ID上达到与完整SFT相当的精度且局部更优，减少65%训练数据。
6. **专精与通用权衡**：RFTs在金融ID上超越Gemini 3.0 Flash（0.718 vs 0.581），但在更广泛的OOD上处于劣势（0.569 vs 0.701），说明任务对齐比模型规模更重要，但通用模型在跨领域上有天然优势。

## 7. 优点

- **方法创新**：首次将GRPO用于文档视觉定位，并设计门控密集奖励防止模型“偷懒”（预测大框），并通过消融严格证明推理不必要的结论。
- **实验严谨**：控制变量（同一奖励、同一骨干），分别分析ID和OOD，动态跟踪检查点，揭示训练过程中的推理压缩和定位发散现象。
- **实用价值**：早期SFT→RL策略显著降低标注成本，且推理压缩带来推理加速（token减少>60%），适合实际部署。
- **新发现**：发现“定位发散”这一OOD下的非对称泛化现象，为后续研究提供方向。

## 8. 不足与局限

- **模型规模局限**：仅评估了4B参数模型，结论在更大模型上是否成立未知，作者承认这是未来工作。
- **推理分析不足**：推理变体仅限冷启动RL，缺少推理标注的SFT→RL流水线，因为当前缺乏这类数据。作者将此列为社区挑战。
- **OOD覆盖有限**：仅使用两个OOD基准（4,828样本），未包含更困难场景（如手写、低质量扫描、多语言文档）。
- **定位发散未解决**：论文仅描述了现象，未提出缓解策略。
- **计算资源未公开训练时长**：虽指出单卡可跑，但未提供具体时间，影响复现参考。
- **外部基线可比性**：Gemini 3.0 Flash和DOGR模型与本文模型在训练数据、规模上差异较大，对比仅作为参考，并非公平竞争。

（完）
