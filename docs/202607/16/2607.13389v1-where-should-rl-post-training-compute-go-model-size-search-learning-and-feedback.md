---
title: "Where Should RL Post-Training Compute Go? Model Size, Search, Learning, and Feedback"
title_zh: 强化学习后训练的计算资源应如何分配？模型规模、搜索、学习与反馈的权衡
authors: "Patrick Wilhelm, Odej Kao"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13389v1"
tags: ["query:rl"]
score: 8.0
evidence: 研究LLM RL后训练中模型规模、搜索、学习和反馈的计算分配
tldr: 强化学习后训练面临固定计算预算下的资源分配问题。本文提出FLOP核算框架，将GRPO后训练计算分解为展开搜索、策略更新和反馈评估三部分。在LoRA微调Qwen2.5策略上实验发现，最优分配随模型大小、预算、奖励系统变化，更大模型因消耗更多每token计算而减少更新或展开次数。RACE诊断协议可帮助识别分配机制，建议论文报告总FLOPs及分配细节。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13389v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1406, \"height\": 1012, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13389v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1409, \"height\": 725, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13389v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1409, \"height\": 979, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13389v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1420, \"height\": 689, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13389v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1238, \"height\": 280, \"label\": \"Table\"}]"
motivation: 在固定后训练计算预算下，研究如何分配计算资源给模型大小、搜索、学习和反馈以最大化性能。
method: 提出FLOP核算框架，将GRPO后训练计算分解为展开搜索、策略更新和奖励反馈评估，并在LoRA微调Qwen2.5策略上实验。
result: 最优分配随模型大小、预算、奖励系统变化；更大模型消耗更多计算，导致更新或展开次数减少；基于规则的奖励将大部分非更新计算用于展开。
conclusion: RACE诊断协议可帮助识别分配机制，建议后训练论文报告总FLOPs及计算分配细节。
---

## 摘要
强化学习后训练越来越多地用于使基础模型适应推理、规划和反馈驱动的机器人学习流程，但受限的后训练资源通常用一个总FLOP预算来概括。我们研究了这种做法背后的固定预算决策问题：在相同的后训练预算下，应该使用更大的策略模型，还是更长时间地训练较小的策略模型，生成更多的展开搜索，或者将计算用于更强的奖励反馈？我们为GRPO后训练引入了一个FLOP核算框架，将计算分解为展开/搜索、策略更新/学习以及奖励或反馈模型评估。在基于LoRA微调的Qwen2.5策略上，我们发现了条件分配边界：最佳观察分配随模型规模、计算预算、奖励系统和评估目标而变化。相同FLOP下的模型规模比较表明，模型选择与训练分配是耦合的，因为更大的策略消耗每个token更多的计算，因此在相同预算下只能进行更少的更新或展开。奖励系统也会改变核算：基于规则的奖励几乎将所有非更新计算用于策略展开，而PRM风格的反馈则将预算的可见部分分配给奖励模型推理。我们提出RACE作为一种诊断性预试验网格协议，而非保证性能提升，用于在昂贵的验证运行之前识别分配机制；我们的结果表明，强化学习后训练论文应报告总FLOP，以及计算在模型规模、搜索、学习和反馈之间的分配情况。

## Abstract
Reinforcement Learning (RL) post-training is increasingly used to adapt foundation models for reasoning, planning, and feedback-driven robot-learning pipelines, but constrained post-training resources are often summarized by a single total FLOP budget. We study the fixed-budget decision problem behind this practice: under the same post-training budget, should one use a larger policy, train a smaller policy longer, generate more rollout search, or spend compute on stronger reward feedback? We introduce a FLOP-accounting framework for GRPO post-training that decomposes compute into rollout/search, policy-update/learning, and reward- or feedback-model evaluation. Across LoRA-adapted Qwen2.5 policies, we find conditional allocation frontiers: the best observed allocation changes with model size, compute budget, reward system, and evaluation target. Same-FLOP model-size comparisons show that model choice and training allocation are coupled because larger policies consume more per-token compute and therefore buy fewer updates or rollouts under the same budget. Reward systems also change the accounting: rule-based rewards spend nearly all non-update compute on policy rollouts, while PRM-style feedback allocates a visible part of the budget to reward-model inference. We present RACE as a diagnostic pilot-grid protocol, not a guarantee of held-out improvement, for identifying allocation regimes before expensive validation runs; our results suggest that RL post-training papers should report total FLOPs together with how compute is divided among model size, search, learning, and feedback.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：强化学习（RL）后训练在基础模型适应推理、规划和机器人学习流程中广泛应用，但当前实践中通常仅报告总 FLOP（浮点运算次数）预算，忽略了计算资源在**模型规模、搜索（rollout）、学习（更新）、反馈（奖励评估）** 之间的分配细节。作者指出，相同总 FLOP 下不同分配可能导致截然不同的性能，而“分配决策”本身是一个关键的系统性问题，尚未被系统研究。
- **核心问题**：在固定后训练计算预算下，应将更多计算分配给更大的策略模型、训练更小模型更长时间、增加 rollout 搜索、还是使用更强的（计算成本更高的）奖励反馈？最优分配如何随模型规模、预算、奖励系统和评估目标变化？
- **整体含义**：RL 后训练不仅是总计算量的函数，更是一个**受约束的资源分配问题**。论文旨在揭示这种分配机制，并呼吁社区在报告中不仅报告总 FLOP，还要分解为搜索、学习、反馈三个组成部分。

## 2. 论文提出的方法论

- **核心思想**：建立 FLOP 核算框架，将 RL 后训练总计算 `Ctotal` 显式分解为三个部分：
  - `Csearch`：rollout（推理）生成的 FLOP，与采样数量和生成长度成正比。
  - `Cupdate`：策略更新（学习）的 FLOP，与更新步数、LoRA 秩和有效后向传播成本相关。
  - `Creward`：奖励/验证器推理的 FLOP，对于基于规则的奖励几乎为零，对于过程奖励模型（PRM）则显著。
- **关键技术细节**：
  - 使用 **GRPO（Group Relative Policy Optimization）** 进行后训练。
  - 策略模型为 **LoRA 微调**的 Qwen2.5 指令微调版本（1.5B、3B、7B）。
  - 定义**更新分数 `ρ = Cupdate / Ctotal`**，作为分配的核心变量：小 `ρ` 表示搜索/奖励计算占比高，大 `ρ` 表示更新计算占比高。
  - 引入 **RACE（Reward-Aware Compute Allocation）** 诊断协议：在有限预算下运行 IsoFLOP 小网格，拟合本地响应曲面（对数更新分数的二次模型），推荐验证分配点；若拟合不稳定则回退到最佳观察点。该协议用于识别分配“机制”，而非保证提升。
- **公式说明**（文字化）：
  - `Ftotal = Frollout + Fupdate + Freward`，其中 `Frollout` 用模型参数和 rollout token 数估计；`Fupdate` 使用有效更新分数 `feff`（含冻结骨干的前向/激活成本）；`Freward` 按奖励模型参数和推理次数估计。

## 3. 实验设计

- **数据集/场景**：
  - **训练**：**Polaris-53K** 数学推理数据集。
  - **下游评估**：**GSM8K** 和 **MATH-500**（各取 250 个样本）。
  - 使用**最终答案精确准确率（pass@1 符号等价性）** 和**通用评判器过程质量分数**作为下游指标。
- **Benchmark 与对比**：
  - 对比**五种奖励系统**：稀疏（sparse）、结构化（structured）、密集（dense）、代理 PRM（proxy PRM）、真实 PRM（real PRM，使用 Qwen2.5-Math-PRM-7B）。
  - 对比**三种模型大小**：1.5B、3B、7B。
  - 对比**多个 LoRA 秩**（r ∈ {8,12,16,24,32,48,64,96,128}）和**更新步数**（按计算带调整）。
  - 在**三个计算带**（C1p1e16、C1p8e16、C2p4e16）下进行 IsoFLOP 固定预算实验。
- **主要实验阶段**：
  - **Stage A**：模型感知的搜索-学习分配（三大模型 × 三个计算带 × 9+个秩）。
  - **Stage B**：奖励条件分配，固定 3B 策略，对比四种奖励（3 计算带 × 9 秩 × 4 奖励 = 108 组）。
  - **Stage D**：真实 PRM 验证（固定 3B 策略，3 计算带 × 9 秩 = 27 组）。
  - **RACE 验证**：在 Stage A 上使用排除秩（160,192,256）进行 held-out 验证。

## 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量、训练总时长。所有 FLOP 估算是基于参数-令牌乘法的理论计算，而非实际硬件时间。
- 强调 FLOP 核算用于**一致相对比较**，未模拟硬件效应（如通信、内存带宽）。

## 5. 实验数量与充分性

- **实验数量**：总计超过 135 组独立运行（Stage A 约不定，Stage B 108 组，Stage D 27 组），加上 RACE 额外验证运行。涵盖多个模型规模、LoRA 秩、更新步数和奖励系统，范围较广。
- **充分性评估**：
  - **优点**：系统地搜索了分配空间（计算带 × 秩 × 奖励），覆盖了从搜索密集到更新密集的连续范围；消融了 LoRA 秩、更新步数、奖励类型，并比较了不同评估目标。
  - **局限性**：限于数学推理任务，非直接机器人控制；使用单一模型家族（Qwen2.5）和单一 RL 算法（GRPO）；只有 2 个采样完成（K=2），未探索更大 K；种子有限；FLOP 核算简化了硬件影响。实验设计整体公平但适用范围不够广泛。

## 6. 论文的主要结论与发现

- **存在结构化分配行为**：固定预算下，不同分配对应不同奖励水平，存在可测量的响应曲面。
- **分配前沿是条件性的**：
  - **模型规模耦合**：更大模型消耗更多每令牌计算，相同 FLOP 预算下只能获得更少 rollout 和更新步，模型选择与训练分配不可分离。
  - **奖励系统改变核算**：基于规则的奖励将几乎全部非更新计算用于 rollout；PRM 类奖励分配相当部分计算给奖励模型推理，导致最佳观察更新分数 `ρ` 从 0.72 降至 0.44–0.45（3B 设置）。
  - **评估目标**：原生训练奖励、下游 pass@1 准确率、通用评判器分数可能选出不同分配（如 pass@1 偏好高更新设置 a180，而评判器偏好中等更新 a100）。
- **RACE 诊断有用但非保证**：RACE 能正确识别高/低更新机制（内部和留秩验证同机制率 1.0），但奖励遗憾非零，不能保证 held-out 改进。

## 7. 优点

- **明确的计算分解**：将总 FLOP 显式拆分为搜索、更新、反馈三个可测量部分，揭示了隐藏的权衡，使“分配”成为可分析的研究变量。
- **条件性视角**：不寻求单一最优配方，而是展示最优分配如何随模型、奖励、目标变化，方法更贴近实际工程决策。
- **RACE 诊断协议**：提供一套成本可控的预试验方法，帮助在昂贵大规模运行前识别分配机制，减少盲目探索。
- **多目标评估**：同时报告原生奖励、下游准确率和通用评判器分数，揭示了目标依赖的重要性，增强了结果的可解释性。

## 8. 不足与局限

- **任务范围狭窄**：仅在数学推理上实验，未涉及直接机器人控制、视觉语言动作策略等实际机器人学习任务，结果迁移性需验证。
- **模型与算法多样性不足**：仅使用 Qwen2.5 模型和 GRPO 算法，未测试其他架构（如 Llama、Mistral）或 RL 变体（如 PPO、DPO）。
- **FLOP 核算简化**：未考虑硬件实际效率（如通信、内存带宽）、梯度检查点、混合精度等；更新有效分数 `feff` 的常数 `a=0.85` 可能不通用。
- **RACE 不保证改进**：held-out 验证中最佳分配未显著超越全局基线，仅作为机制诊断而非性能提升方法。
- **实验参数有限**：固定 K=2 和 L=2048，未探索更多采样或更短/更长完成的影响；仅使用单一种子（未提及多次重复）。
- **计算资源未透明**：未报告实际 GPU 小时数，影响可复现性和资源需求评估。

（完）
