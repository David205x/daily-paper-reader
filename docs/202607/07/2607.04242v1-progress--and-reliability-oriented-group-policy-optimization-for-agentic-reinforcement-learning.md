---
title: Progress- and Reliability-Oriented Group Policy Optimization for Agentic Reinforcement Learning
title_zh: 面向进度与可靠性的智能体强化学习分组策略优化
authors: "Mingxuan Fan, Peiyang Liu"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04242v1"
tags: ["query:rl"]
score: 9.0
evidence: 面向LLM智能体的步骤级分组强化学习
tldr: 在长程交互任务中，基于组的强化学习逐步优化大语言模型代理，但步级优势估计易受分组方式影响。为此提出ProGPO，一种无学习批判的上下文一致步级学习方法，通过保持精确前缀动作比较并用基于rollout状态势的转移信用补充稀疏的同伴比较，同时采用语义扩展与逆方差融合可靠估计势。在ALFWorld和WebShop任务上，ProGPO在可比计算开销下超越匹配的代理RL基线，且扩展到大模型仍有效。该方法为代理强化学习提供了更鲁棒的步级策略优化框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04242v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1437, \"height\": 360, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04242v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1421, \"height\": 553, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04242v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1425, \"height\": 791, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04242v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1450, \"height\": 451, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04242v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1435, \"height\": 530, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04242v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1460, \"height\": 733, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04242v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1061, \"height\": 471, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04242v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1454, \"height\": 298, \"label\": \"Table\"}]"
motivation: 现有步级组RL中优势估计对分组方式敏感，导致比较不公平或同伴信号缺失。
method: ProGPO结合精确前缀动作比较与基于状态势的转移信用，通过语义扩展和逆方差融合可靠估计势。
result: 在ALFWorld和WebShop上，ProGPO在Qwen2.5-1.5B/3B模型上超越基线，计算开销相当。
conclusion: ProGPO提供了一种无批判的上下文一致步级学习框架，提升了代理RL的鲁棒性和可扩展性。
---

## 摘要
基于分组的强化学习已成为在长程交互任务中改进大型语言模型智能体的有效范式。为获得比轨迹级优化更细粒度的策略更新，近期研究转向步级分组强化学习，即在一个采样批次内对中间步骤进行分组并比较。然而，步级优势估计对分组方式敏感：按广义状态键分组可提高覆盖率，但可能比较不同历史下的动作；而强制历史一致性可提供更公平的比较，但代价是分组碎片化及缺失同行比较信号。本文提出ProGPO（面向进度与可靠性的分组策略优化），一种无需学习批评器的上下文一致步级学习方法。ProGPO保持精确前缀动作比较，并利用基于采样的状态势能推导出的转移信誉来补充稀疏的同行比较。为可靠估计这些势能，ProGPO结合语义扩展与跨历史深度的逆方差融合。我们在两个具有挑战性的智能体任务ALFWorld和WebShop上，使用Qwen2.5-1.5B-Instruct进行评估。结果表明，在相当的计算开销下，ProGPO优于匹配的智能体强化学习基线，而额外的Qwen2.5-3B-Instruct实验进一步测试了所提方法的可扩展性。

## Abstract
Group-based reinforcement learning (RL) has become an effective paradigm for improving large language model agents on long-horizon interactive tasks. To obtain finer-grained policy updates than trajectory-level optimization, recent work has moved toward step-level group-based RL, where intermediate steps are grouped and compared within a rollout batch. However, step-level advantage estimation is sensitive to how groups are formed: grouping by broad state keys improves coverage but may compare actions taken under different histories, while enforcing historical consistency yields fairer comparisons at the cost of fragmented groups and missing peer-comparison signal. In this paper, we propose ProGPO (Progress- and Reliability-Oriented Group Policy Optimization), a learned-critic-free method for context-consistent step-level learning. ProGPO keeps exact-prefix action comparison, and complements sparse peer comparisons with transition credit derived from rollout-based state potentials. To estimate these potentials reliably, ProGPO combines semantic expansion with inverse-variance fusion across history depths. We evaluate ProGPO on two challenging agentic tasks, ALFWorld and WebShop, with Qwen2.5-1.5B-Instruct. Results show that ProGPO improves over matched agentic RL baselines under comparable computational overhead, and additional Qwen2.5-3B-Instruct experiments further test the scalability of the proposed method.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题背景**：大语言模型作为智能体在长程交互任务（如网页导航、具身家务等）中面临长期规划、错误恢复和稀疏延迟奖励下的信用分配挑战。
- **现有方法局限**：基于组的强化学习（如GRPO）在轨迹级优化，而步级分组（如GiGPO、HGPO）可实现更细粒度更新，但步级优势估计对分组方式敏感：按广义状态键分组会混入不同历史下的动作，导致比较不公平；而强制历史一致性（精确前缀匹配）虽使比较更公平，却导致大量步骤成为孤立组或低对比组，缺乏同伴比较信号（论文诊断显示33.7%~44.5%的步骤无有效同伴比较）。
- **核心动机**：如何在保持动作比较公平性的同时，从同一采样批次中恢复更密集的学习信号，无需额外训练价值网络或收集更多轨迹。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：提出ProGPO（Progress- and Reliability-Oriented Group Policy Optimization），将动作比较与状态进度估计分离。动作比较保持精确前缀一致性；而通过非参数化的状态势能（state potential）从批量采样统计中推导转移信用（transition credit），为同伴比较无法覆盖的步骤提供学习信号。
- **关键技术细节**：
  - **前缀一致性步级分组**：按长度为k的精确观测前缀形成组，计算组内相对优势 \( A_t^{(k)} \)，并加权聚合所有有效深度得到层次优势 \( A_t^{\text{hier}} \)。
  - **非参数化状态势能估计**：对每个前缀组，用平均回报估计势能 \( \hat{V}^{(k)}(p_t^{(k)}) \)。
  - **可靠性感知多分辨率融合**：将不同历史深度视为一组估值器，用逆方差加权（权重 \( w_k = n_k / (\sigma_k^2 + \epsilon_{\text{var}}) \)）融合多深度势能，得到 \( \hat{V}^{\text{mr}}(s_t) \)，使样本更多、方差更低的深度贡献更大。
  - **语义扩展**：当当前观测组样本少时，用策略隐藏状态嵌入的余弦相似度检索最近邻状态，并加权平均其回报，稳定势能估计（仅用于状态势能，不用于动作比较）。
  - **转移信用**：计算步骤前后状态势能差 \( A_t^{\text{trans}} = \hat{V}^{\text{mr}}(s_{t+1}) - \hat{V}^{\text{mr}}(s_t) \)，作为动作推进进度的信号。
  - **互补信用组合**：最终优势 \( A_t^{\text{ProGPO}} = A_t^{\text{hier}} + \lambda_{tr} \cdot \eta_t \cdot A_t^{\text{trans}} \)，其中 \( \eta_t \) 根据同伴信号是否存在调整转移信用的保留比例。
  - **优化目标**：使用标准裁剪代理目标（PPO风格），辅以KL惩罚。

## 3. 实验设计：数据集/场景、Benchmark、对比方法
- **数据集/场景**：
  - **ALFWorld**：具身家庭任务（物品拾取、清洁、冷却、查看、加热、双物品等），每回合最多50步。
  - **WebShop**：模拟电商购物，按指令搜索购买商品，最多30步。
- **基准（Baseline）**：对比方法包括：
  - 提示方法（GPT-4o、Gemini-2.5-Pro、Qwen2.5、ReAct、Reflexion）——引用自先前报告作为参考。
  - RL方法：PPO、RLOO、GRPO、GiGPO、HGPO（均为局部复现或引用）。
- **模型**：主要实验用Qwen2.5-1.5B-Instruct；可扩展性实验用Qwen2.5-3B-Instruct。
- **评价指标**：任务成功率和任务分数（WebShop分数）。

## 4. 资源与算力
论文未明确说明使用的GPU型号、数量或训练总时长。仅在训练细节中提到：批量大小16，采样组大小8，学习率1e-6，KL系数0.01，使用vLLM进行轨迹生成，训练步数160步。未提供完整的算力消耗统计。

## 5. 实验数量与充分性
- **主要实验**：在1.5B模型上，对ALFWorld和WebShop两个任务，每个方法使用3个不同随机种子进行验证，结果取均值和标准差（表1）。共对比了GRPO、GiGPO、HGPO和ProGPO（加上引用的提示/PPO/RLOO结果）。
- **可扩展性实验**：在3B模型上对ALFWorld进行训练端比较（GiGPO、HGPO、ProGPO），报告训练成功率、验证成功率和验证测试分数（仅文字描述）。
- **消融实验**：对ALFWorld的转移信用组件进行消融（表3），包括：仅转移信用、+语义扩展、+可靠性融合、+两者组合。在训练第160步测量。
- **机制诊断**：图5和表2分析了步骤中同伴比较、转移信用覆盖的比例等。
- **充分性评价**：实验覆盖了两个代表性的长程交互环境，并进行了多种子验证、可扩展性测试和关键组件消融，设计较为全面。但所有实验均基于Qwen2.5模型系列，未测试其他架构（如Llama等）或更多任务（如WebArena、SWE-bench等），存在一定局限性。

## 6. 主要结论与发现
- ProGPO在ALFWorld总体成功率上达到90.1%，优于GRPO (75.8%)、GiGPO (85.5%) 和 HGPO (87.8%)。
- 在WebShop上，ProGPO取得最高成功率71.5%，GiGPO在任务分数上略高（85.8 vs 85.4）。
- 在3B模型上，ProGPO在最后检查点（步155）的训练成功率、验证成功率和验证测试分数均高于GiGPO和HGPO。
- 机制诊断显示：转移信用补充了约11.4%的“仅转移”步骤，且与同伴比较共同覆盖了43.0%（WebShop）~63.3%（ALFWorld）的步骤；多分辨率融合在82.5%（WebShop）~73.3%（ALFWorld）的值查找中被使用。
- 消融表明：同时使用语义扩展和可靠性融合可获得最佳转移信用效果（训练成功率96.9%）。

## 7. 优点
- **创新性**：提出了一种无需学批评器（critic）的步级信用分配框架，通过非参数化状态势能和转移信用，在保持动作比较公平性的同时缓解了稀疏信号问题。
- **实用性**：不增加额外rollouts或价值网络，利用已有采样批次统计，计算开销与基线相当。
- **鲁棒性**：逆方差融合和语义扩展有效稳定了势能估计，减少噪声。
- **诊断深入**：详细分析了步骤覆盖情况，展示了方法在真实训练中的行为，使读者理解其工作原理。

## 8. 不足与局限
- **残留零信号步骤**：仍有部分步骤（WebShop 22.3%，ALFWorld 33.1%）在信用组合后仍为零，说明无法覆盖所有情况。
- **表示质量依赖**：非参数化势能表继承状态表示的质量，当观测频繁唯一或语义检索可能引入行为不相似的状态时，势能估计仍可能噪声。
- **因果关系限制**：转移信用是批量估计的进度信号，而非因果效应估计，可能受环境随机性、任务难度或表示误差影响。
- **实验覆盖面有限**：仅在ALFWorld和WebShop两个环境、Qwen2.5系列模型（最大3B）上测试，未包含更多样化的智能体任务（如开放性网页任务、多模态智能体）或更大模型（如7B+）。超参数（如历史长度、语义邻居数、信用系数等）的系统性扫描未进行。
- **算力资源未报告**：缺少详细的GPU型号、数量、训练时间等信息，影响可复现性和成本评估。

（完）
