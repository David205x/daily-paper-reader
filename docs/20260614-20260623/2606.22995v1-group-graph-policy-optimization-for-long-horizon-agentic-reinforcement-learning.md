---
title: Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning
title_zh: 面向长视界自主智能体强化学习的群图策略优化
authors: "Yunan Wang, Minghui Song, Zihan Zhang, Shaohan Huang, Haizhen Huang, Furu Wei, Weiwei Deng, Feng Sun, Qi Zhang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22995v1"
tags: ["query:rl"]
score: 9.0
evidence: 面向长链代理RL的图结构策略优化方法
tldr: "针对长程智能体强化学习中奖励稀疏延迟和序列轨迹忽略图结构的问题，提出G2PO算法，将交互轨迹转化为全局状态转移图，通过分组聚合观测和边优势估计降低方差并全局分配信用。在WebShop、ALFWorld等基准上超越SOTA，最高提升22.2%，为复杂多步决策任务提供有效范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22995v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 689, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 688, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 393, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1447, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 555, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 699, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 728, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 277, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22995v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 832, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22995v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 590, \"height\": 323, \"label\": \"Table\"}]"
motivation: 现有步级RL方法忽视状态图结构，导致高方差估值和短视信用分配，难以处理长期依赖。
method: 构建全局状态转移图，分组聚合观测降低方差，基于边的优势估计实现全局信用分配。
result: "在WebShop、ALFWorld等基准上显著超越GRPO等方法，成功率提升最高22.2%。"
conclusion: G2PO通过图结构建模和全局优势估计有效解决长程奖励稀疏问题，提升智能体决策质量。
---

## 摘要
基于群体的强化学习（RL）显著增强了自主智能体场景下的大规模语言模型（LLM）。为实现更细粒度的策略更新，最近的自主智能体RL框架已从轨迹级训练转向步级训练。然而，长视界自主智能体RL面临严重的奖励稀疏性和延迟问题，因为反馈往往在数十个交互步骤之后才出现。尽管现有的步级框架优化了训练粒度，但其信用分配仍然是粗粒度的，并且仍将智能体探索视为孤立的线性轨迹。这种过度简化的视角忽略了状态转移固有的图结构，导致高方差的状态价值估计和短视、局部的信用分配。为克服这些关键瓶颈，我们提出了群图策略优化（G2PO），这是一种专为多轮自主智能体任务设计的新型基于群体的RL算法。G2PO明确地将线性交互轨迹转换为全局状态转移图。通过聚合不同轨迹中的相同观测，我们引入了群体聚合状态价值估计，从而降低了采样方差和轨迹相关偏差。此外，我们重新定义了智能体动作作为状态节点之间的转移，并提出了一种以边为中心的优势估计策略。通过全局标准化整个图上的时序差分（TD）误差，G2PO明确识别并优先处理驱动绝对任务进展的关键转移。在代表性的长视界基准测试——WebShop、ALFWorld和AppWorld上的大量实验表明，G2PO显著优于最先进的基于提示的和RL基线，相较于GRPO实现了高达22.2%的成功率提升。

## Abstract
Group-based Reinforcement Learning (RL) has significantly enhanced Large Language Models (LLMs) in agentic scenarios. To achieve finer-grained policy updates, recent agentic RL frameworks have shifted from trajectory-level to step-level training. However, long-horizon agentic RL suffers from severe reward sparsity and delay, as feedback is often deferred for dozens of interaction steps. While existing step-level frameworks refine training granularity, their credit assignment remains coarse-grained and still treats agent exploration as isolated, linear trajectories. This oversimplified perspective ignores the inherent graph structure of state transitions, leading to high-variance state-value estimation and myopic, localized credit assignment. To overcome these critical bottlenecks, we propose Group-Graph Policy Optimization (G2PO), a novel group-based RL algorithm tailored for multi-turn agentic tasks. G2PO explicitly transforms linear interaction trajectories into a global state-transition graph. By aggregating identical observations across different trajectories, we introduce group-aggregation state-value estimation that reduces sampling variance and trajectory-dependent bias. Furthermore, we redefine agent actions as transitions between state nodes and propose an edge-centric advantage estimation strategy. By globally standardizing Temporal Difference (TD) errors across the entire graph, G2PO explicitly identifies and prioritizes critical transitions that drive absolute task progress. Extensive experiments on representative long-horizon benchmarks-WebShop, ALFWorld, and AppWorld-demonstrate that G2PO substantially outperforms state-of-the-art prompt-based and RL baselines, achieving remarkable success rate improvements of up to 22.2% over GRPO.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、深入、客观的中文总结。

### 论文总结：群图策略优化（G2PO）用于长视界自主智能体强化学习

#### 1. 核心问题与整体含义（研究动机和背景）

*   **核心问题**：在长视界的自主智能体（Agent）任务中，强化学习（RL）面临严重的**奖励稀疏**和**延迟**问题（智能体需执行数十步后才获得反馈）。同时，现有的步级（step-level）训练方法虽然比轨迹级（trajectory-level）更精细，但仍存在两大关键缺陷：
    *   **高方差的价值估计**：将每个轨迹孤立看待，导致从相同状态出发的不同路径，其价值估计仅依赖于单次随机结果，方差极高。
    *   **短视的、局部的信用分配**：仅在同一局部状态下比较不同动作的优劣（本地归一化），无法区分一个“微小改进”和一个“突破性进展”动作对于全局目标的绝对贡献。
*   **研究含义**：论文认为，长视界任务的本质是一个**状态转移图**，而非孤立的线性轨迹。忽视这一图结构是现有方法效果不佳的根本原因。

#### 2. 论文提出的方法论：G2PO 算法

论文提出 **G2PO（Group-Graph Policy Optimization）**，一种基于群体的、面向图结构的强化学习算法。其核心思想是将采样的多条交互轨迹重构为一个统一的状态转移图，并在此基础上进行细粒度的信用分配。

*   **核心思想与关键技术**：
    1.  **状态群图构建**：
        *   将多条不同轨迹中的**相同观测（状态）进行聚类**，形成“状态群（State Group）”作为图的节点。
        *   智能体的**动作**被视为连接不同状态群节点的**有向边**。
    2.  **群体聚合状态价值估计**：
        *   修正了单轨迹回报的随机性。对于每个状态群节点，其价值 `**V (状态群)**` 定义为所有经过该节点的轨迹中，从该节点开始到最终轨迹结束的**折扣回报的均值**。
        *   这显著降低了价值估计的方差，因为一个状态的价值由其所有可能路径的平均结果决定，而非一次随机结果。
    3.  **多粒度优势估计**：
        *   **节点中心优势（Node-Centric Advantage, `A_NC`）**：在一个状态群内部，将当前动作导致的下一状态群的价值，与该节点所有可能动作导致的下一状态群价值进行局部归一化。这解决了“在当前状态下，哪个动作更好”的问题。
        *   **边中心优势（Edge-Centric Advantage, `A_EC`）**：为解决局部归一化的短视，G2PO计算每条“边”（即动作 `a`）的 **1步时序差分误差 `δ = V(下一状态群) - V(当前状态群)`**。然后，**对所有轨迹中的所有边的TD误差进行全局标准化**。这能识别并放大那些带来绝对价值飞跃的“关键突破动作”。
        *   **回合级优势（Episode-Level Advantage, `A_EP`）**：保留原始GRPO的全局奖励归一化，确保优化不偏离最终目标。
    4.  **总体优势**：最终的步级优势由三者加权求和： `A = A_EP + w * (A_NC + A_EC)`。

*   **公式/算法流程（文字说明）**：
    *   **采样**：使用旧策略 `π_old` 采样 `N` 条完整轨迹。
    *   **构图**：对所有步骤的观测进行聚类，构建状态群图。
    *   **估值**：计算每个状态群的价值 `V(状态群)`（由所有经过该群的轨迹的折扣回报平均得到）。
    *   **计算优势**：
        *   计算每条边（动作）的1步TD误差 `δ`。
        *   基于 `δ` 计算全局标准化的 `A_EC`。
        *   基于群内比较计算 `A_NC`。
        *   基于最终奖励计算 `A_EP`。
        *   加权求和得到最终的 `A`。
    *   **更新**：使用类似GRPO的截断代理目标函数（clipped surrogate objective）更新策略 `π_θ`。

#### 3. 实验设计

*   **数据集/场景**：三个具有代表性的长视界智能体基准测试：
    *   **WebShop**：模拟电子商务网站，评估智能体搜索、导航和购买商品的能力。
    *   **ALFWorld**：基于文本的具身模拟环境，涵盖6类家务任务（如取物、加热、清洗等）。
    *   **AppWorld**：复杂的软件生态系统，智能体需通过Python API调用与多种App交互，挑战性更高。
*   **Benchmark**：主要指标为 **成功率（Success Rate）** 和 **得分（Score）**。
*   **对比方法**：
    *   **前沿离线模型**：Qwen3.5-397B, DeepSeek-V3.2, Gemini-2.5-Pro。
    *   **提示学习方法**：ReAct, Reflexion。
    *   **RL训练方法**：PPO, RLOO, GRPO, GiGPO。
*   **基础模型**：主要在 Qwen2.5-1.5B-Instruct 和 Qwen2.5-7B-Instruct 上进行训练，AppWorld使用了 Qwen2.5-14B-Instruct。

#### 4. 资源与算力

*   **明确说明**：论文在实验细节部分说明了算力资源。
    *   **Qwen2.5-1.5B/7B 实验**：使用 **4 × H100 GPU**，运行 **100 次迭代**。
    *   **Qwen2.5-14B 实验**：使用 **8 × H100 GPU**，运行 **50 次迭代**。
    *   **训练时长**：论文未明确报告每轮训练的具体耗时。

#### 5. 实验数量与充分性

*   **实验数量**：较为充分，主要包含以下几类：
    *   **主实验结果**：在3个不同基准上、2种（3种）模型规模下与6种RL方法对比。
    *   **消融实验**：系统地在 `A_EP` 基础上，逐一添加 `A_NC`、群体聚合（GA）估值、和 `A_EC` 三个组件，证明了每个组件的必要性。
    *   **分析实验**：包括超参数 `w` 分析、状态价值方差分析、任务效率分析、计算开销分析。
*   **充分性与公平性**：
    *   **充分**：实验覆盖了多种任务类型（电商、具身、软件操作）、多种模型规模（1.5B, 7B, 14B），并与当时最新的多种方法对比，消融实验设计清晰。
    *   **公平**：所有RL方法共用相同的超参数和提示词，实验进行多次（3个随机种子）并报告均值与标准差，保证了比较的客观性。GRPO和GiGPO作为强基线，G2PO的显著提升展示了其优势。

#### 6. 论文的主要结论与发现

*   **G2PO 显著优于所有基线**：在WebShop、ALFWorld和AppWorld上，G2PO的成功率和得分均大幅超越基于提示的方法及其他RL训练方法。
*   **具体提升**：与基线GRPO相比，G2PO在WebShop上成功率提升最高达 **22.2%**，在ALFWorld上提升最高达 **14.4%**。
*   **核心贡献的有效性**：
    *   **群体聚合估值**：实验证明能有效降低状态价值估计的方差。
    *   **边中心优势**：通过全局标准化，能精准识别和强化对达成目标有巨大贡献的“关键动作”。
*   **效率优势**：G2PO训练的模型能以更少的交互步数完成任务，且其额外计算开销极低（仅占0.4%的训练时间），几乎可以忽略不计。

#### 7. 优点

*   **方法论创新性强**：巧妙地将图结构思想引入LLM的步级RL训练中，解决了现有方法忽略全局结构的问题，视角新颖。
*   **信用分配精准**：通过群体聚合消除随机噪声，并通过“边中心”的全局标准化，实现了对长链任务中关键动作的准确识别和奖励，这是该工作的核心亮点。
*   **实践效果好且成本低**：在多个主流基准上大幅超越SOTA，同时计算开销几乎没有增加，实用性强。
*   **理论分析扎实**：在附录中给出了方差缩减的数学证明，增强了方法的可信度。

#### 8. 不足与局限

*   **框架依赖性**：论文承认，实验仅在 `verl-agent` 一个框架上实施，在不同Agent RL框架下的泛化能力有待验证。
*   **状态聚类假设**：方法依赖于“相同观测”的聚类。在观测高度复杂、动态变化（如视觉图像）或语义相同但文本表述多样的环境中，精确的聚类可能成为瓶颈，需要更鲁棒的状态表征方法。
*   **实验规模的限制**：虽然使用了14B模型，但对于当前前沿的大模型（如70B+），其扩展性和有效性未经验证。
*   **潜在的偏差风险**：实验中使用的规则化奖励系统（成功=10，失败=0，无效动作惩罚=-0.1）相对简单。在更复杂的奖励函数下，方法的鲁棒性有待研究。

（完）
