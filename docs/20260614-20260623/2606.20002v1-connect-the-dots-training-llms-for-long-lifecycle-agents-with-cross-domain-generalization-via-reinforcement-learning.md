---
title: "Connect the Dots: Training LLMs for Long-Lifecycle Agents with Cross-Domain Generalization Via Reinforcement Learning"
title_zh: 连接点：通过强化学习训练LLM以实现跨域泛化的长生命周期智能体
authors: "Yanxi Chen, Weijie Shi, Yuexiang Xie, Boyi Hu, Yaliang Li, Bolin Ding, Jingren Zhou"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20002v1"
tags: ["query:rl"]
score: 9.0
evidence: 端到端强化学习训练LLM以支持长生命周期代理
tldr: 长期生命周期AI代理需在环境中连续执行任务并自我更新，但LLM缺乏这种元能力。本文提出CoD框架，通过端到端RL训练，让LLM学会在任务解决与环境探索中自我更新上下文。采用GRPO-style算法和细粒度信用分配，在多个域内及跨域验证了泛化能力，并扩展到Ralph-loop设置。该框架连接了多项先前工作，为LLM代理研究开辟了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20002v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1658, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1608, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1523, \"height\": 1466, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1527, \"height\": 1463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1360, \"height\": 1272, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20002v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1561, \"height\": 1453, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 815, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 545, \"label\": \"Table\"}]"
motivation: 现有LLM缺乏长期生命周期中自我更新和跨域泛化的元能力，需要统一框架进行训练。
method: 提出CoD框架，包括端到端RL训练（GRPO-style算法）、任务环境设计，以及解耦任务解决与上下文更新的交互序列。
result: 在训练域内、跨域及从CoD到Ralph-loop设置均观察到有效的泛化能力，验证了元能力的迁移性。
conclusion: CoD框架为训练具备长期生命周期能力的LLM代理提供了可扩展方案，并开源实现以促进后续研究。
---

## 摘要
这项工作提出了一个通用框架，用于训练大型语言模型（LLM）具备“连接点”（CoD）这一元能力，这是长生命周期智能体所需的：当基于LLM的AI智能体部署在环境中时，它会解决一长串任务，同时持续探索环境、从自身经验中学习，并迭代地自我更新关于环境的上下文，从而在基于更新上下文的未来任务上逐步获得更好的性能。CoD框架的主要组成部分包括：（1）用于端到端强化学习（RL）的算法设计和基础设施，其中长展开序列交织求解任务和更新上下文的片段；（2）用于在训练过程中激励和激发LLM中目标元能力、以及在评估过程中忠实衡量进展的任务和环境。我们展示了CoD框架的概念验证实现，包括具有细粒度信用分配的GRPO风格RL算法，以及针对目标元能力（而非特定领域LLM能力或标准逐任务RL）量身定制的任务和环境。实证结果验证了端到端RL训练在CoD设置中的有效性，并展示了所激发的元能力在训练领域内、跨不同领域以及从CoD到Ralph-loop设置中的分布外泛化潜力。我们对CoD的研究连接了先前的一系列工作，并为推进LLM和AI智能体开辟了新的机会。为了促进进一步的研究和应用，我们在\url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod}上发布了我们的实现。

## Abstract
This work presents a general framework for training large language models (LLMs) to "Connect the Dots" (CoD), a meta-capability required by long-lifecycle agents: as an LLM-based AI agent gets deployed in an environment, it solves a long sequence of tasks while continuously exploring the environment, learning from its own experiences, and iteratively self-updating its context about the environment, thereby achieving progressively better performance on future tasks conditioned on the updated context. Major components of the CoD framework include: (1) algorithm design and infrastructure for end-to-end reinforcement learning (RL) with long rollout sequences interleaving solve-task and update-context episodes; (2) tasks and environments for incentivizing and eliciting the targeted meta-capability in LLMs during training, as well as for faithfully measuring progress during evaluation. We present proof-of-concept implementations of the CoD framework, including a GRPO-style RL algorithm with fine-grained credit assignment, as well as tasks and environments tailored to the targeted meta-capability (rather than domain-specific LLM capabilities or standard task-by-task RL). Empirical results validate the efficacy of end-to-end RL training in the CoD setting, and demonstrate the potential for out-of-distribution generalization -- within the training domains, across different domains, and from CoD to Ralph-loop settings -- of the elicited meta-capability. Our investigation of CoD connects several lines of prior works, and opens up new opportunities for advancing LLMs and AI agents. To facilitate further research and applications, we release our implementations at \url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod}.

---

## 论文详细总结（自动生成）

## 论文总结：Connect the Dots: Training LLMs for Long-Lifecycle Agents with Cross-Domain Generalization Via Reinforcement Learning

### 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：当前 LLM 部署为长生命周期智能体时，缺乏一种关键元能力——“连接点”（CoD）。该能力要求智能体在环境中执行一长串不同但相关的任务时，能持续探索、从自身经验中学习、迭代地自我更新上下文，从而在后续任务中获得更好性能。现有 LLM 在未明确训练的情况下，容易在欠规范环境中迷失，通常需要人工设计的复杂智能体框架才能稳定运行。
- **整体含义**：通过端到端强化学习（RL）直接训练 LLM 获取 CoD 元能力，有望让智能体在部署中自动适应新环境、自我改进，并泛化到不同领域，推动 LLM 从单一任务执行者向长期自适应智能体进化。

### 2. 方法论：核心思想、关键技术细节
- **核心思想**：将长期部署过程抽象为交替执行“求解任务”和“更新上下文”两个类型的片段，并利用 RL 训练 LLM 以最大化长期累积奖励。框架名为 CoD，包含 **CoD-Deploy**（部署抽象）和 **CoD-Train**（RL 训练）。
- **关键技术细节**：
  - **RL 算法**：采用 GRPO 风格，无需 Critic 模型。每条轨迹包含多个求解任务片段（每个有任务奖励）和更新上下文片段（仅小格式奖励）。对每个片段定义回报为当前及未来所有求解任务奖励的平均值，以同位置片段的平均回报作为基线计算优势，实现细粒度信用分配。
  - **梯度更新**：采用 **REC-OneSide-NoIS** 变体（删除重要性采样权重），并加入自适应重加权（RED-Weight）机制：若批次平均优势为负，则用指数权重 `exp(Ai/T)` 重新平衡正负梯度，温度 T 通过二分法使得加权平均优势接近 0，且限制 T > 0.8 避免数值不稳定。
  - **环境与任务设计**：设计了三个专门环境以激励 CoD 元能力：
    - **FrozenLake-Obscure**：经典冻结湖游戏，但动作（A/B/C/D）与方向（上下左右）的映射随机且隐藏，必须通过探索揭示。
    - **Alchemy-Random**：元素合成游戏，元素和配方随机生成，需自行发现有效组合。
    - **TerminalSimulator**：模拟终端操作（文件传输等），环境特性（如操作系统）需探索。
  - **上下文更新机制**：快速实现为单段文本“提示（hint）”，由 LLM 在更新上下文片段中自动生成并添加到后续任务系统提示词后。

### 3. 实验设计
- **数据集/场景**：
  - 训练：FrozenLake-Obscure（单独）或该域与 Alchemy-Random 混合（任务序列长度 4）。
  - 评估：域内 OOD（更难实例，更大地图/更多元素，序列长度 8）；跨域 OOD（未训练过的 TerminalSimulator）；Ralph-loop 设置（同一任务重复求解）。
- **基准模型**：起始模型为 Qwen3-8B-Instruct（禁用思考模式）。
- **对比方法**：主要对比了三种 RL 梯度计算方法：原始 GRPO（带重要性采样）、REC-OneSide-NoIS（无重要性采样）、以及最终算法（添加 RED-Weight 重加权）。未与标准逐任务 RL（从零求解每个任务）在相同序列设置下直接对比（论文指出其天然不匹配 CoD）。
- **关键指标**：每个位置（序列中第几个任务）的平均奖励（任务求解成功率）。

### 4. 资源与算力
- **明确说明**：论文仅给出了部分超参数（附录 B），包括学习率 1e-6、批次大小、最大令牌数等，但 **未明确说明所使用的 GPU 型号、数量以及训练时长**。推测使用了阿里云的计算资源，但细节缺失。

### 5. 实验数量与充分性
- **实验组数**：两组主要训练设置（A：单一冰冻湖域；B：混合域）。每组包含多条训练曲线和评估曲线（域内 OOD、跨域 OOD、Ralph-loop）。附录中还比较了三种 RL 算法在 Alchemy-Random 上的稳定性（图 5）。
- **充分性分析**：
  - **优点**：涵盖了域内、跨域、特殊设置（Ralph-loop）的泛化评估，验证了 CoD 元能力的迁移性；对比了不同 RL 算法组件，消融了稳定性问题。
  - **不足**：实验规模有限（仅 8B 模型，序列长度 4/8）；未与现有长期智能体框架（如 Voyager、Mem0）或基于外部记忆的方法比较；仅使用简单“提示”作为上下文，未验证更复杂记忆/技能文件场景。实验覆盖度尚可但不够全面。

### 6. 主要结论与发现
- **训练有效性**：端到端 RL 训练显著提升 CoD 元能力。例如在 FrozenLake-Obscure 中，序列第 0 个任务成功率从 18% 升至 45%（受信息论极限限制），而第 4 个任务成功率从 28% 升至 76%。
- **泛化能力**：
  - **域内 OOD**：在更难实例（更大地图/更长序列）上仍持续提升。
  - **跨域 OOD**：在未训练过的 TerminalSimulator 上（CoD-Deploy 和 Ralph-loop 设置）均观察到性能改善。
  - **Ralph-loop**：从 CoD 到 Ralph-loop 的泛化得到验证（后者可视为 CoD 的特例：同一任务重复求解）。
- **RL 算法设计**：细粒度信用分配和自适应重加权对稳定训练至关重要。

### 7. 优点
- **框架通用性**：CoD 框架抽象了长期部署过程，易于扩展至不同环境和上下文管理机制。
- **端到端 RL 训练**：直接优化元能力，减少人工设计智能体框架的需求。
- **细粒度信用分配**：考虑未来奖励，适应序列任务场景，比简单求和更有效。
- **跨域泛化验证**：证明了元能力可迁移到未见过的环境和 Ralph-loop 设置，潜力较大。
- **开源实现**：提供代码以促进后续研究。

### 8. 不足与局限
- **实验规模有限**：仅使用 8B 模型，序列长度较短（4/8），未扩展到更大模型和更长序列。
- **环境设计简单**：三个环境（特别是冷冻湖和炼金术）相对简单，距离真实复杂应用（个人助手、代码仓库维护）尚有差距。
- **上下文机制简化**：仅用单段文本“提示”，未涉及持久记忆库、技能文件等更丰富机制，可能限制复杂场景效果。
- **RL 算法仍有启发式成分**：自适应重加权（RED-Weight）缺乏严格理论推导，稳定性在高维域中可能仍不足（混合训练时出现波动）。
- **缺乏强基线对比**：未与现有长期智能体方法（如 Voyager、免梯度在线学习、外部记忆+固定 LLM 的方案）进行量化比较，削弱说服力。
- **未集成到 LLM 后训练流水线**：论文仅提出概念验证，未讨论如何与现有 SFT/RLHF 流程高效融合。

（完）
